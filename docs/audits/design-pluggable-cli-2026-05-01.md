## Run: 2026-05-01T00:00:00Z

**Gaps found: 5**

1. **`ErrorEvent` struct missing `retryable` field** — The ADR defines `ErrorEvent` with only `Message` and `Code` fields. The downstream `spec-nats-payload-schema.md` (which declares a dependency on this ADR for the canonical event schema) defines the error event JSON payload as including `"retryable": true`. A developer implementing the `ErrorEvent` struct from ADR-0005 would produce a type that doesn't match the wire format described in the payload schema.
   - **Doc**: `ErrorEvent` struct under "Canonical Event Schema" — missing `Retryable bool` field
   - **Code**: `spec-nats-payload-schema.md` error event example includes `"retryable": true` with description "Whether the agent will automatically retry"

2. **NATS Subject Structure section lists only 2 of 9+ per-session subjects** — The "NATS Subject Structure" section shows only `sessions.{sslug}.events` and `sessions.{sslug}.input`. The target architecture in `spec-nats-data-taxonomy.md` and `spec-nats-payload-schema.md` (both depending on this ADR) define 7 additional per-session subjects: `.delete`, `.config`, `.control.interrupt`, `.control.restart`, `.lifecycle.started`, `.lifecycle.stopped`, `.lifecycle.error`. The `SessionInput` type then includes `InputInterrupt` (with comment "from control.interrupt subject") and `InputConfig` — referencing subjects never listed in the ADR's NATS section. A developer reading only the ADR would miss these subjects and would not know how interrupts, config patches, deletes, restarts, and lifecycle notifications are routed.
   - **Doc**: "NATS Subject Structure" section — only `.events` and `.input` listed
   - **Code**: `spec-nats-data-taxonomy.md` Resource Hierarchy shows `.delete`, `.config`, `.control.interrupt`, `.control.restart`, `.lifecycle.started`, `.lifecycle.stopped`, `.lifecycle.error`; `spec-nats-payload-schema.md` defines payloads for all of them

3. **`SessionInput` is ambiguous — wire format or internal dispatch type?** — The section header states "User inputs arrive on `sessions.{sslug}.input` and are typed commands" — implying a single NATS subject carries all input types. But `InputInterrupt` has the comment `// from control.interrupt subject; agent sends SIGINT`, and the payload schema defines `sessions.{sslug}.control.interrupt` with an empty payload (`{}`), `sessions.{sslug}.config` with patch-semantics JSON, and `sessions.{sslug}.delete` with empty payload. A developer cannot determine from the ADR whether `SessionInput` is the wire format for the `.input` subject (all types multiplexed) or an internal type the session agent constructs after demultiplexing from multiple subjects. If it's internal, the `id`/`ts` envelope fields and type-discriminated payload make no sense for subjects like `.control.interrupt` which carry empty payloads.
   - **Doc**: "Session Input Schema" — "User inputs arrive on `sessions.{sslug}.input`" followed by `InputInterrupt InputType = "interrupt" // from control.interrupt subject`
   - **Code**: `spec-nats-payload-schema.md` defines `sessions.{sslug}.control.interrupt` with empty payload `{}`; `sessions.{sslug}.config` with its own JSON shape

4. **GenericTerminalDriver base64 `text_delta` has no encoding indicator** — The ADR states PTY output is "published as `text_delta` events with base64-encoded content (binary-safe)." But `TextDeltaEvent` has only `MessageID`, `BlockIndex`, and `Text string` — no field to signal base64 encoding vs plain UTF-8 text. For structured backends (`hasEventStream: true`), the SPA renders `text_delta` as conversation text; for the PTY backend (`hasEventStream: false`), the SPA must base64-decode the same field into raw terminal bytes. The ADR does not describe how the consumer distinguishes these two decoding modes at the event level, or whether a flag (e.g., `encoding: "base64"`) should be added to `TextDeltaEvent`.
   - **Doc**: GenericTerminalDriver section — "Raw bytes read from the PTY master are published as `text_delta` events with base64-encoded content"
   - **Code**: `TextDeltaEvent` struct — only `MessageID`, `BlockIndex`, `Text` fields; no encoding indicator

5. **`CLICapabilities` described as superset of `Capabilities` but struct lacks old fields** — The ADR states: "The existing `Capabilities` struct (`{skills, tools, agents}`) in `state.go` is replaced by `CLICapabilities`, which is a superset (it includes `hasThinking`, `hasSubagents`, etc. alongside the tool/skill/agent lists carried in `InitEvent`)." The `CLICapabilities` struct definition does NOT include `Skills []string`, `Tools []string`, or `Agents []string` — those are on `InitEvent`. The current code (`state.go:56-60`) stores `Capabilities{Skills, Tools, Agents}` in the session KV entry. After the replacement, a developer would not know whether to: (a) add the lists to `CLICapabilities` (contradicting the struct), (b) move them to top-level `SessionState` fields (not mentioned), or (c) drop them from KV entirely (breaking the current contract where the SPA reads them from KV).
   - **Doc**: Data Model Changes section — "is replaced by `CLICapabilities`, which is a superset"
   - **Code**: `mclaude-session-agent/state.go:56-60` — `Capabilities struct { Skills []string; Tools []string; Agents []string }`; `CLICapabilities` struct definition in ADR has no equivalent fields
