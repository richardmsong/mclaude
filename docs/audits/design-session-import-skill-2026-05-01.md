## Run: 2026-05-01T00:00:00Z (Round 1)

**Gaps found: 7**

1. **`importRef` value type is ambiguous — S3 key vs import ID** — The ADR says `importRef` stored in project KV state is an "S3 key" (Component Changes: "creates project in Postgres with `source: 'import'` and `import_ref` (S3 key), writes `ProjectKVState` with `importRef`"). But the session-agent retrieves the download URL via `GET /api/imports/{id}/download-url` where `{id}` is an opaque import ID (e.g. `imp-001`), not an S3 key. The agent reads `importRef` from KV to call this endpoint — if it contains an S3 key (e.g. `alice/laptop-a/myapp/imports/imp-001.tar.gz`), it cannot be used as the `{id}` path parameter. Either `importRef` should store the import ID (and the Postgres `import_ref` column stores the S3 key separately), or the download endpoint should accept the S3 key. A developer implementing the session-agent import handler would have to guess.
   - **Doc**: "Extended `ProjectKVState` / `ProjectState` with `importRef` field (S3 key, replaces `ImportObjectRef`)" and "If `importRef` is set: requests pre-signed download URL from CP (`GET /api/imports/{id}/download-url`)"
   - **Code**: `ProjectState` in `mclaude-common/pkg/types/types.go:134` has no `importRef` field yet (new addition). The HTTP endpoint path uses `{id}` which conventionally refers to the resource ID, not an S3 key path.

2. **Import completion mechanism contradicts between ADR and payload schema** — ADR-0053 step 15 says the agent performs two actions: (a) clears `importRef` from project KV state itself, and (b) signals CP to delete the S3 object via `DELETE /api/imports/{id}` (HTTP). But `spec-nats-payload-schema.md` defines a NATS subject `mclaude.users.{uslug}.hosts.{hslug}.projects.{pslug}.import.complete` where the agent publishes completion and "CP updates the project state in KV (`importStatus: 'complete'`)." These describe the same operation with different mechanisms and different actors clearing the KV state. A developer implementing this would not know: (a) does the agent clear importRef from KV, or does CP? (b) does the agent call HTTP DELETE, or publish to NATS import.complete, or both?
   - **Doc**: User Flow step 15: "Session-agent clears `importRef` from project KV state, signals CP to delete the S3 object (`DELETE /api/imports/{id}`)"
   - **Code**: `spec-nats-payload-schema.md` line ~1060 defines `import.complete` subject with payload `{s3Key, bytesDownloaded, filesExtracted}` where "CP updates the project state in KV"

3. **check-slug NATS subject format inconsistent** — ADR-0053 step 5 uses `mclaude.users.{uslug}.api.projects.check-slug` (user-scoped, no host token). But `spec-nats-activity.md` section 11-pre uses `mclaude.users.alice.hosts.laptop-a.projects.check-slug` (host-scoped). Project slug uniqueness in Postgres is `UNIQUE (user_id, host_id, slug)` — checking slug availability requires knowing the host. The user-scoped subject in ADR-0053 omits the host, so CP cannot validate uniqueness without the host slug being in either the subject or the payload. Neither is specified.
   - **Doc**: User Flow step 5: "Checks slug availability via NATS request/reply to control-plane (`mclaude.users.{uslug}.api.projects.check-slug`)"
   - **Code**: `spec-nats-activity.md:884` uses host-scoped variant; `spec-state-schema.md` Postgres projects table has `UNIQUE (user_id, host_id, slug)` constraint

4. **Session-agent has no mechanism to discover CP HTTP URL** — The agent calls HTTP endpoints on the control-plane (`GET /api/imports/{id}/download-url`, `GET /api/attachments/{id}`, `DELETE /api/imports/{id}`). But neither the session-agent spec nor ADR-0053 specifies how the agent discovers the CP HTTP base URL. There is no env var defined for it (the existing agent config table has `NATS_URL` but no CP HTTP URL). In K8s mode, the CP runs in `mclaude-system` namespace — the agent would need a service DNS name or env var. In BYOH/daemon mode, the daemon has `REFRESH_URL` but the child agent doesn't inherit an HTTP base URL for CP. ADR-0054 (declared dependency) introduces HTTP auth for agents but also doesn't specify URL discovery.
   - **Doc**: Component Changes — mclaude-session-agent: "requests pre-signed download URL from CP (`GET /api/imports/{id}/download-url`)"
   - **Code**: `mclaude-session-agent` configuration table in spec has no CP HTTP URL knob; `REFRESH_URL` is daemon-only and used only for JWT refresh, not general CP API calls

5. **CLI config file path contradicts existing spec** — ADR-0053 says the CP API URL is configured via `~/.mclaude/config.json` (set by `mclaude config set api-url <url>`). The existing CLI spec (`docs/mclaude-cli/spec-cli.md`) says the server URL comes from `~/.mclaude/context.json`'s `server` key. A developer implementing `mclaude login` would not know which file to read, and `mclaude config set` is a new subcommand not listed in the CLI spec's command table.
   - **Doc**: "CLI bootstrapping: configured via `~/.mclaude/config.json` (set by `mclaude config set api-url <url>`) with a default of `https://api.mclaude.internal`"
   - **Code**: `docs/mclaude-cli/spec-cli.md` — `mclaude login` flags: "`--server <url>` — Read from `~/.mclaude/context.json`'s `server` key"

6. **spec-nats-activity.md import walkthrough contradicts ADR transport choices** — `spec-nats-activity.md` section 11 shows the full import flow using NATS request/reply for upload URL (`import.request`), confirmation (`import.confirm`), and download URL (`import.download`). ADR-0053 specifies these as HTTP endpoints (`POST /api/imports/upload-url`, `POST /api/imports/{id}/confirm`, `GET /api/imports/{id}/download-url`). The activity spec is not listed in the ADR's Impact section as requiring updates, so these contradictory NATS subjects will persist alongside the ADR's HTTP endpoints. A developer reading both documents would have to guess which transport is canonical.
   - **Doc**: ADR-0053 Impact section lists 8 specs to update; `spec-nats-activity.md` is absent
   - **Code**: `docs/spec-nats-activity.md:884-928` defines NATS subjects `import.request`, `import.confirm`, `import.download` that the ADR replaces with HTTP

7. **`mclaude login` device-code flow relationship to existing login unclear** — The CLI spec already defines `mclaude login` as email+password or OAuth, storing a bearer token at `~/.mclaude/auth.json`. ADR-0053 introduces a device-code flow for `mclaude login` that stores NATS credentials (JWT + NKey seed) at the same path. The ADR says "New `mclaude login` subcommand" but the subcommand already exists with different behavior and different output format. A developer would need to know: does the device-code flow replace or augment the existing login? Is the bearer token format backward-compatible with the new NATS credential format? What happens to existing `auth.json` files containing bearer tokens?
   - **Doc**: Component Changes: "New `mclaude login` subcommand: Initiates device-code flow…Stores credentials at `~/.mclaude/auth.json`"
   - **Code**: `docs/mclaude-cli/spec-cli.md` — existing `mclaude login`: "Prompts for email + password (or opens an OAuth browser flow)…writes it to `~/.mclaude/auth.json`"

## Run: 2026-05-01T01:00:00Z (Round 2)

Round 1 gap status:
- R1-G1 (`importRef` ambiguity): **FIXED** — ADR now clearly says KV `importRef` = import ID (e.g. `imp-001`), Postgres `import_ref` = S3 key. The parenthetical in the mclaude-common section explicitly distinguishes them.
- R1-G2 (import completion contradicts): **FIXED** — ADR step 15 now says "publishes `import.complete` via NATS; CP receives signal and deletes the S3 object." The CP component changes note explicitly says "S3 object deletion is triggered by the agent's `import.complete` NATS publication (no HTTP DELETE endpoint needed for agent-to-CP)."
- R1-G3 (check-slug subject): **FIXED** — Step 5 now uses host-scoped subject: `mclaude.users.{uslug}.hosts.{hslug}.projects.check-slug`. The text adds: "Slug uniqueness is per-user-per-host, so the host token is required in the subject."
- R1-G4 (agent CP HTTP URL): **FIXED** — Agent component changes now consistently say "via NATS request/reply" for both import download and attachments. No HTTP URL needed.
- R1-G5 (CLI config file path): **FIXED** — ADR now says `~/.mclaude/context.json`'s `server` key, consistent with the CLI spec.
- R1-G6 (transport inconsistency): **PARTIALLY FIXED** — `spec-nats-activity.md` is now in the ADR's Impact section. However, the ADR's own User Flow and Component Changes still describe HTTP for CLI import operations while the spec uses NATS. See new gap 1 below.
- R1-G7 (`mclaude login` device-code): **FIXED** — ADR now says "Updated `mclaude login` subcommand (extends existing command…)" and explicitly states "this replaces the previous email+password/OAuth flow." Credential format change is documented: "credential format changes from bearer token to NATS JWT + NKey seed."

**Gaps found: 4**

1. **CLI import transport still contradicts between ADR body and spec-nats-activity.md** — The ADR's User Flow (steps 8, 10) and Component Changes (CLI section: "Requests pre-signed upload URL: `POST /api/imports/upload-url`"; CP section: "New import HTTP endpoints: `POST /api/imports/upload-url`, `POST /api/imports/{id}/confirm`") describe HTTP endpoints for CLI-to-CP import operations. But `spec-nats-activity.md` section 11a (listed in the ADR's Impact section as updated by this ADR) describes the same operations as NATS request/reply subjects: `mclaude.users.alice.hosts.laptop-a.projects.myapp.import.request` and `mclaude.users.alice.hosts.laptop-a.projects.myapp.import.confirm`. A developer implementing the CLI import command would not know whether to make HTTP calls or NATS request/reply calls. The ADR is also internally inconsistent: step 4 connects to NATS and step 5 uses NATS for slug check, but steps 8-10 switch to HTTP without explanation.
   - **Doc**: User Flow step 8: "CLI requests pre-signed upload URL from CP: `POST /api/imports/upload-url`"; CP Component Changes: "New import HTTP endpoints"
   - **Code**: `docs/spec-nats-activity.md:907-918` (section 11a) uses NATS subjects `import.request` and `import.confirm` — both documents claim to be updated by ADR-0053

2. **No NATS subjects defined for agent attachment operations** — The session-agent Component Changes say: "When processing a user input with `AttachmentRef`: requests download URL from CP via NATS request/reply" and "When agent generates binary output: requests upload URL from CP via NATS request/reply, uploads to S3." But no NATS subject is defined anywhere for these operations. The import flow has explicit NATS subjects (`import.download`, `import.complete`), but attachments have only HTTP endpoints (`POST /api/attachments/upload-url`, `POST /api/attachments/{id}/confirm`, `GET /api/attachments/{id}`). The `spec-nats-activity.md` subject namespace map (updated by this ADR) includes `import.*` subjects but no `attachment.*` subjects. The `spec-nats-payload-schema.md` defines only HTTP endpoints for attachments. A developer implementing the session-agent attachment handler cannot proceed without knowing what NATS subject to publish/request on.
   - **Doc**: Session-agent Component Changes: "requests download URL from CP via NATS request/reply"; Attachment Download flow step 2: "Agent requests pre-signed download URL from CP via NATS request/reply (agent-to-CP communication is always NATS, never HTTP)"
   - **Code**: `docs/spec-nats-activity.md` subject namespace map has no `attachment.*` entries; `docs/spec-nats-payload-schema.md` "HTTP Endpoints (attachment lifecycle)" defines only HTTP paths

3. **Device-code auth endpoint path prefix inconsistency** — The ADR specifies device-code auth endpoints as `POST /auth/device-code`, `POST /auth/device-code/poll`, and `GET /auth/device-code/verify` (under the `/auth/` public namespace). But `spec-nats-activity.md` section 2f (updated by this ADR per Impact) uses `POST /api/auth/device-code` (under `/api/auth/`). The CP spec's existing endpoint table has public auth at `/auth/*` and protected API at `/api/*` — these are different routing namespaces with different auth middleware. A developer wiring the route handler would not know which prefix is correct.
   - **Doc**: ADR CLI Component Changes: "Initiates device-code flow: `POST /auth/device-code`"; ADR CP Component Changes: "`POST /auth/device-code`"
   - **Code**: `docs/spec-nats-activity.md:172` uses `POST /api/auth/device-code` — same ADR, different path prefix

4. **`import.complete` payload inconsistent across specs updated by this ADR** — `spec-nats-payload-schema.md` defines the `import.complete` payload as `{id, ts, s3Key, bytesDownloaded, filesExtracted}` with `s3Key` as a required field. `spec-nats-activity.md` section 11d shows an empty payload `{}` for the same subject. The ADR itself (step 15) does not specify the payload at all. All three documents were updated by this ADR (spec-nats-payload-schema.md and spec-nats-activity.md are in the Impact section). Additionally, if the payload requires `s3Key`, the agent has no obvious way to obtain it — the agent only stores the import ID from `importRef` in project KV, and CP stores the S3 key in Postgres. The agent would need to parse the S3 key from the pre-signed download URL, which is fragile.
   - **Doc**: ADR step 15: "publishes `import.complete` via NATS" (no payload); spec-nats-activity.md section 11d: `{}` (empty)
   - **Code**: `docs/spec-nats-payload-schema.md` `import.complete` section defines `{s3Key, bytesDownloaded, filesExtracted}` as the payload

## Run: 2026-05-01T02:00:00Z (Round 3)

Round 2 gap status:
- R2-G1 (CLI import transport contradicts): **FIXED** — ADR User Flow steps 8 and 10 now use NATS request/reply (`import.request`, `import.confirm` subjects) throughout. CP Component Changes explicitly state "CLI-to-CP and agent-to-CP operations use NATS, not HTTP." No HTTP endpoints remain for import operations. `spec-nats-activity.md` section 11a is consistent (NATS subjects).
- R2-G2 (No NATS subjects for agent attachment operations): **FIXED** — CP Component Changes now define three explicit NATS request/reply subjects for agent-to-CP attachment operations: `attachments.download`, `attachments.upload`, `attachments.confirm` (all under `mclaude.users.{uslug}.hosts.{hslug}.projects.{pslug}.attachments.*`). The User Flow — Attachments sections reference these subjects with request/response payloads.
- R2-G3 (Device-code auth endpoint path prefix inconsistency): **FIXED** — ADR now consistently uses `/api/auth/device-code` prefix in both CLI and CP Component Changes. `spec-nats-activity.md` section 2f matches.
- R2-G4 (`import.complete` payload inconsistency): **PARTIALLY FIXED** — ADR step 15 now explicitly defines the payload as standard envelope only: `{"id":"...","ts":...}` (no additional fields). However, `spec-nats-payload-schema.md` (listed in Impact) still defines `{id, ts, s3Key, bytesDownloaded, filesExtracted}`, and `spec-nats-activity.md` section 11d shows `{}` (empty). See new gap 2 below.

**Gaps found: 2**

1. **Device-code login NKey handling contradiction** — The ADR's `mclaude login` CLI Component Changes say the device-code poll response is `{ jwt, nkeySeed, userId, userSlug, natsUrl }`, and in the same bullet says "CLI generates its own NKey pair, sends public key, receives JWT." These contradict: if the CLI generates the NKey pair, `nkeySeed` should not be in the poll response (the CLI already has the seed, and seeds must never leave the generating device). If the CP generates the pair and returns `nkeySeed`, the CLI should not be "generating its own NKey pair." Additionally, the `POST /api/auth/device-code` request body is not specified — only the response `{ deviceCode, userCode, verificationUrl, expiresIn, interval }` is shown. The CLI "sends public key" but there is no `nkeyPublic` field in either the device-code initiation or poll request payloads. `spec-nats-activity.md` section 2f (updated by this ADR per Impact) shows the correct pattern: CLI sends `{"nkey_public":"UDEF..."}` in the initiation request, poll returns `{"jwt":"...","userSlug":"alice"}` (no seed), further confirming the ADR body has the wrong response shape. A developer implementing the device-code endpoints would not know: (a) who generates the NKey pair, (b) when the public key is transmitted to CP, or (c) whether the poll response includes the seed.
   - **Doc**: CLI Component Changes: "Receives `{ jwt, nkeySeed, userId, userSlug, natsUrl }` — this replaces the previous email+password/OAuth flow. CLI generates its own NKey pair, sends public key, receives JWT."
   - **Code**: `docs/spec-nats-activity.md:170-180` (section 2f) shows CLI sending `nkey_public` in initiation, poll returning `{jwt, userSlug}` only — no `nkeySeed` in the response

2. **`import.complete` payload still contradicts across specs updated by this ADR** — ADR step 15 now explicitly defines the payload as standard envelope only: `{"id":"...","ts":...}` with the note "(no additional fields — the subject encodes user/host/project, and CP resolves the import ID from project KV state)." But `spec-nats-payload-schema.md` (listed in the Impact section as updated by this ADR) defines the same subject's payload as `{id, ts, s3Key, bytesDownloaded, filesExtracted}` — three additional fields beyond the envelope. `spec-nats-activity.md` section 11d (also in Impact) shows an empty payload `{}`. All three documents are current and updated by this ADR, but describe three different payloads for the same subject. A developer implementing the agent (publisher) following the ADR would send `{"id":"...","ts":...}`, while a developer implementing the CP (subscriber) following the payload schema would expect `s3Key`, `bytesDownloaded`, and `filesExtracted` — the payloads don't match.
   - **Doc**: ADR step 15: `{"id":"...","ts":...}` (standard envelope only); `spec-nats-activity.md` section 11d: `{}` (empty)
   - **Code**: `docs/spec-nats-payload-schema.md` line ~1070 defines `{id, ts, s3Key, bytesDownloaded, filesExtracted}` as the payload — contradicts the ADR's "no additional fields"

## Run: 2026-05-01T03:00:00Z (Round 4)

Round 3 gap status:
- R3-G1 (Device-code NKey handling contradiction): **FIXED** — The ADR's CLI Component Changes now clearly state: "Receives `{ jwt, userSlug }` — the CLI generates its own NKey pair locally. It sends only the public key in the device-code request. The poll response returns the signed JWT and user slug — no seed, no NKey material from the server." The `auth.json` stores `{ jwt, nkeySeed, userSlug }` where `nkeySeed` is the locally-generated seed, not received from the server. This is internally consistent. `spec-nats-activity.md` section 2f also matches: CLI sends `{"nkey_public":"UDEF..."}` in initiation, poll returns `{"jwt":"...","userSlug":"alice"}`.
- R3-G2 (`import.complete` payload contradicts): **FIXED** — All three sources now agree. ADR step 15: `{"id":"...","ts":...}` (standard envelope only). `spec-nats-payload-schema.md` line 1060+: `{"id":"01JTRK8MH...","ts":1714470087000}` with text "Standard envelope only — no additional fields." `spec-nats-activity.md` section 11d: `{"id":"01JTRK8MH...","ts":1714470095000}` — matches the standard envelope.

**Gaps found: 4**

1. **ADR-0054 stale `ImportObjectRef` reference** — ADR-0054 line 396 still contains a comment "clear ImportObjectRef" (`$KV.mclaude-projects-alice.hosts.laptop-a.projects.myapp  # KV write: update this project's state (e.g., clear ImportObjectRef)`). ADR-0053 explicitly says "`importRef` replaces `ImportObjectRef`" and the Impact section states ADR-0054 was updated to "remove all NATS Object Store references (`OBJ_mclaude-imports-{uslug}`, `$O.*` permissions, one-shot import JWTs, `import_jwt` field)." The `ImportObjectRef` type name was part of the Object Store design and should have been updated to `importRef`. A developer reading ADR-0054's agent JWT example would see a stale type name that doesn't match any type defined in ADR-0053 or mclaude-common.
   - **Doc**: ADR-0053 Impact: "remove all NATS Object Store references" from ADR-0054; ADR-0053 mclaude-common: "`importRef` field (import ID, e.g. `imp-001`; replaces `ImportObjectRef`)"
   - **Code**: `docs/adr-0054-nats-jetstream-permission-tightening.md:396` still uses `ImportObjectRef`

2. **spec-state-schema.md not updated despite being listed in Impact** — The ADR's Impact section claims `docs/spec-state-schema.md` was updated with "extended project KV state schema (`importRef` replaces `ImportObjectRef`)." However, the state schema's `ProjectState` KV value (lines 176-186) does NOT include `importRef` or any import-related field. Additionally, the Postgres `projects` table (lines 35-44) does not include the `source` or `import_ref` columns that the ADR defines ("Extended `ProjectState` Postgres model with `source` and `import_ref` columns"). The `attachments` Postgres table defined in the ADR is also absent from the state schema. A developer implementing the CP database layer would find no schema for imports or attachments in the canonical state reference.
   - **Doc**: ADR-0053 Impact: "`docs/spec-state-schema.md` — extended project KV state schema (`importRef` replaces `ImportObjectRef`)"; ADR-0053 mclaude-common: "Extended `ProjectState` Postgres model with `source` and `import_ref` columns"
   - **Code**: `docs/spec-state-schema.md` `mclaude-projects` KV section (line 176) and Postgres `projects` table (line 35) — no `importRef`, `source`, `import_ref`, or `attachments` table

3. **spec-nats-activity.md subject namespace map missing attachment NATS subjects** — The ADR defines three NATS request/reply subjects for agent-to-CP attachment operations: `attachments.download`, `attachments.upload`, `attachments.confirm` (under `mclaude.users.{uslug}.hosts.{hslug}.projects.{pslug}.attachments.*`). The ADR's Impact section says `spec-nats-activity.md` was updated. The subject namespace map at the end of spec-nats-activity.md (lines 1180-1225) lists the `import.*` subjects but omits all `attachments.*` subjects. A developer consulting the canonical subject tree would not know these subjects exist.
   - **Doc**: ADR-0053 CP Component Changes: "`attachments.download` (subject: `mclaude.users.{uslug}.hosts.{hslug}.projects.{pslug}.attachments.download`)"; Impact: `spec-nats-activity.md` updated
   - **Code**: `docs/spec-nats-activity.md` subject namespace map (lines 1183-1225) — no `attachments.*` entries

4. **spec-nats-payload-schema.md missing NATS subject definitions for imports and attachments** — The ADR defines six NATS request/reply subjects: `import.request`, `import.confirm`, `import.download` (for imports) and `attachments.download`, `attachments.upload`, `attachments.confirm` (for attachments). The ADR's Impact section lists `spec-nats-payload-schema.md` as updated. However, the payload schema spec only defines `import.complete` as a NATS subject (line 1060). The other five subjects have no payload schema definition. Additionally, the attachment section in the payload schema only defines HTTP endpoints (`POST /api/attachments/upload-url`, `POST /api/attachments/{id}/confirm`, `GET /api/attachments/{id}`) — these are the SPA-to-CP paths, not the agent-to-CP NATS paths. A developer implementing the CLI import handler would find no payload schema for `import.request` or `import.confirm`; a developer implementing the agent attachment handler would find no payload schema for `attachments.download`, `attachments.upload`, or `attachments.confirm`.
   - **Doc**: ADR-0053 CP Component Changes lists all six NATS subjects with inline payload descriptions; Impact lists `spec-nats-payload-schema.md` as updated
   - **Code**: `docs/spec-nats-payload-schema.md` — only `import.complete` NATS subject defined (line 1060); attachment section (lines 1176-1240) defines only HTTP endpoints
