# kubectl apply -f secret.yaml
# Replace the placeholder values with real tokens before applying.
# Generate tokens:  openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: mclaude-relay-secrets
  namespace: mclaude
type: Opaque
stringData:
  TUNNEL_TOKEN: "REPLACE_WITH_SECURE_RANDOM_TOKEN"
  WEB_TOKEN:    "REPLACE_WITH_SECURE_RANDOM_TOKEN"
