apiVersion: v1
kind: Service
metadata:
  name: mclaude-relay
  namespace: mclaude
spec:
  selector:
    app: mclaude-relay
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
