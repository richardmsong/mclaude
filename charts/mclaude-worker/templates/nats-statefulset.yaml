{{- if .Values.nats.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mclaude-worker.fullname" . }}-nats
  namespace: {{ include "mclaude-worker.namespace" . }}
  labels:
    {{- include "mclaude-worker.labels" . | nindent 4 }}
    app.kubernetes.io/component: nats
spec:
  replicas: {{ .Values.nats.replicas }}
  serviceName: {{ include "mclaude-worker.fullname" . }}-nats-headless
  selector:
    matchLabels:
      {{- include "mclaude-worker.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: nats
  template:
    metadata:
      labels:
        {{- include "mclaude-worker.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: nats
    spec:
      {{- include "mclaude-worker.podSecurityContext" . | nindent 6 }}
      {{- include "mclaude-worker.imagePullSecrets" . | nindent 6 }}
      terminationGracePeriodSeconds: 60
      containers:
        - name: nats
          image: {{ include "mclaude-worker.image" (dict "imageValues" .Values.nats.image "global" .Values.global) }}
          imagePullPolicy: {{ .Values.nats.image.pullPolicy }}
          {{- include "mclaude-worker.securityContext" . | nindent 10 }}
          ports:
            - name: client
              containerPort: {{ .Values.nats.service.clientPort }}
              protocol: TCP
            - name: monitor
              containerPort: {{ .Values.nats.service.monitorPort }}
              protocol: TCP
            - name: websocket
              containerPort: {{ .Values.nats.service.websocketPort }}
              protocol: TCP
          args:
            - "-c"
            - "/etc/nats/nats.conf"
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          volumeMounts:
            - name: config
              mountPath: /etc/nats
            {{- if or .Values.leafCreds.existingSecret .Values.leafCreds.jwt }}
            - name: leaf-creds
              mountPath: /etc/nats/leaf-creds
              readOnly: true
            {{- end }}
            - name: trust
              mountPath: /etc/nats/trust
              readOnly: true
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /healthz
              port: monitor
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz?js-enabled=true
              port: monitor
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.nats.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "mclaude-worker.fullname" . }}-nats-config
        {{- if or .Values.leafCreds.existingSecret .Values.leafCreds.jwt }}
        - name: leaf-creds
          secret:
            {{- if .Values.leafCreds.existingSecret }}
            secretName: {{ .Values.leafCreds.existingSecret }}
            {{- else }}
            secretName: {{ include "mclaude-worker.fullname" . }}-nats-leaf-creds
            {{- end }}
        {{- end }}
        - name: trust
          secret:
            secretName: {{ include "mclaude-worker.fullname" . }}-nats-trust
        {{- if not .Values.nats.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
  {{- if .Values.nats.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.nats.persistence.storageClass }}
        storageClassName: {{ .Values.nats.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.nats.persistence.size }}
  {{- end }}
{{- end }}
