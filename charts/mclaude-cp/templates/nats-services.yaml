{{- if .Values.nats.enabled }}
## Client-facing service (ClusterIP) — includes leafnodes port for worker clusters
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mclaude-cp.fullname" . }}-nats
  namespace: {{ include "mclaude-cp.namespace" . }}
  labels:
    {{- include "mclaude-cp.labels" . | nindent 4 }}
    app.kubernetes.io/component: nats
spec:
  type: ClusterIP
  selector:
    {{- include "mclaude-cp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: nats
  ports:
    - name: client
      port: {{ .Values.nats.service.clientPort }}
      targetPort: client
      protocol: TCP
    - name: websocket
      port: {{ .Values.nats.service.websocketPort }}
      targetPort: websocket
      protocol: TCP
    - name: monitor
      port: {{ .Values.nats.service.monitorPort }}
      targetPort: monitor
      protocol: TCP
    - name: leafnodes
      port: {{ .Values.nats.leafNodes.listenPort }}
      targetPort: leafnodes
      protocol: TCP
---
## Headless service for StatefulSet DNS
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mclaude-cp.fullname" . }}-nats-headless
  namespace: {{ include "mclaude-cp.namespace" . }}
  labels:
    {{- include "mclaude-cp.labels" . | nindent 4 }}
    app.kubernetes.io/component: nats
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    {{- include "mclaude-cp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: nats
  ports:
    - name: client
      port: {{ .Values.nats.service.clientPort }}
      targetPort: client
      protocol: TCP
{{- end }}
