## Run: 2026-04-14T00:00:00Z

GAP: "control-plane ClusterRole: resources: [\"deployments\", \"deployments/scale\"]" → chart ClusterRole only lists `deployments`, omitting `deployments/scale` subresource (charts/mclaude/templates/clusterrole.yaml:22-24)

GAP: "control-plane ClusterRole: - apiGroups: [\"networking.k8s.io\"] resources: [\"networkpolicies\"] verbs: [\"get\", \"list\", \"create\", \"update\", \"delete\"]" → ClusterRole has no networkpolicies rule at all (charts/mclaude/templates/clusterrole.yaml — missing entirely)

GAP: "livenessProbe: httpGet: path: /health port: 8080 periodSeconds: 15" → chart uses path /healthz and periodSeconds: 30 (values.yaml:130, control-plane-deployment.yaml:112-115)

GAP: "readinessProbe: httpGet: path: /ready # checks Postgres connection only port: 8080 periodSeconds: 10" → chart uses path /readyz (values.yaml:132)

GAP: "DEV_OAUTH_TOKEN" / values.yaml comment: "When non-empty, a dev-oauth-token key is added to the control-plane Secret and DEV_OAUTH_TOKEN is set on the control-plane container" → the Secret is never created or patched by any Helm template; the comment describes behavior that is not implemented (values.yaml:118-119, no corresponding Secret template exists)

GAP: "DEV_OAUTH_TOKEN" env var is set as a plain inline value (`value: {{ .Values.controlPlane.devOAuthToken | quote }}`), not sourced from the externally-managed Secret via `secretKeyRef` — the token is exposed in the Deployment spec rather than kept in a Secret (charts/mclaude/templates/control-plane-deployment.yaml:106-109)

GAP: NATS configmap has no `operator`, `resolver`, or authentication configuration — the spec describes NATS JWT authentication with an Operator NKey and per-user JWTs enforced at the broker, which requires `operator` and `resolver` directives in nats.conf (charts/mclaude/templates/nats-configmap.yaml — missing entirely)
