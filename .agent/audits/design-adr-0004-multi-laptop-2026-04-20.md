## Run: 2026-04-20T00:00:00Z

**Gaps found: 8**

1. **`pkg/subj` still uses old signatures — the ADR's changes are not implemented** — The ADR states "Old `UserProject*` helpers are removed (not deprecated — hard cutover, no dual-path)" and all project-scoped helpers gain an `h HostSlug` parameter. The actual `mclaude-common/pkg/subj/subj.go` still has the old two-arg signatures (`UserProjectAPISessionsInput(u, p)`, etc.), old KV key helpers without `hslug` (`SessionsKVKey(u, p, s)`, `ProjectsKVKey(u, p)`), `LaptopsKVKey` instead of `HostsKVKey`, and the old filter constants (`FilterMclaudeAPI = "mclaude.users.*.projects.*.api.sessions.>"`, etc.). A developer implementing the ADR needs to know that this is the starting state and every call site in `mclaude-session-agent/daemon_jobs.go`, `agent.go`, `state.go`, `daemon.go`, and `mclaude-control-plane/projects.go` uses the old helpers and must be updated. The ADR does not enumerate all call sites, making it unclear whether the developer should update the helpers first and then fix call sites, or do a combined commit.
   - **Doc**: "Old `UserProject*` helpers are removed (not deprecated — hard cutover, no dual-path)." / "JetStream filter constants updated in this step (before components that reference them)"
   - **Code**: `mclaude-common/pkg/subj/subj.go:18-24` — filter constants still `mclaude.users.*.projects.*...`; `subj.go:66-127` — all project-scoped helpers are two-arg (u, p) with no host param; `daemon_jobs.go:342,438,635,647` — all call sites use old two-arg form

2. **`users` table has no `slug` column — reconciler cannot resolve `USER_SLUG` or `HOST_SLUG` to inject into pod env** — The ADR states "Reconciler: resolves `HOST_SLUG` from Postgres alongside `USER_SLUG` and `PROJECT_SLUG` when building pod templates." The spec-state-schema.md Deployment section says the reconciler resolves `USER_SLUG`, `HOST_SLUG`, and `PROJECT_SLUG` from `users.slug`, `hosts.slug`, `projects.slug`. However, the actual `db.go` `schema` constant has no `slug` column in `users`, and the `User` struct has no `Slug` field. Neither `projects` nor a `hosts` table exists. The ADR and schema both presuppose a `GetUserBySlug`/`GetHostBySlug` capability that does not exist in the codebase. The developer would need to add `slug` to the `users` schema in `db.go`, add getter methods, and also add all `hosts`/`projects.host_id` — but the exact migration path for the already-live `users` table (backfilling `slug` for existing rows) is not covered by the ADR's migration section, which only covers `hosts` and `projects.host_id`.
   - **Doc**: "Reconciler: resolves `HOST_SLUG` from Postgres alongside `USER_SLUG` and `PROJECT_SLUG` when building pod templates."
   - **Code**: `mclaude-control-plane/db.go:313-349` — `schema` constant for `users` table has no `slug` column; `reconciler.go:343-346` — env only injects `USER_ID`, `PROJECT_ID`, `NATS_URL`; no `USER_SLUG`, `PROJECT_SLUG`, or `HOST_SLUG` injected

3. **No `slug` column in the existing Postgres `users` table — backfill strategy for existing user rows is unspecified** — The ADR defines a `MigrateHosts(ctx)` function covering `hosts` table creation, `projects.host_id`, and the project backfill. But the `users` table must also gain a `slug` column (it exists in `spec-state-schema.md` as `UNIQUE NOT NULL` and is required for all NATS subjects, KV keys, and the HTTP URL scheme). This DDL step (add `slug`, derive values for existing users via `DeriveUserSlug`, set NOT NULL) is absent from the migration DDL section entirely. A developer implementing the migration would stop here.
   - **Doc**: Migration DDL section covers only `hosts` table and `projects.host_id`; `users.slug` not mentioned.
   - **Code**: `db.go:314-322` — `CREATE TABLE IF NOT EXISTS users` has no `slug` column; `spec-state-schema.md:18` — `slug TEXT UNIQUE NOT NULL` is listed as a required column.

4. **`session-agent/main.go` hardcoded lifecycle subject uses old pre-host format and `HOST_SLUG` env var does not exist** — The ADR states: "`main.go` hardcoded lifecycle init subject ... replaced with `subj.UserHostProjectLifecycle(u, h, p, "_init")`; `HOST_SLUG` must be read from env before this call site." The actual `main.go:200` has `subject := fmt.Sprintf("mclaude.users.%s.projects.%s.lifecycle._init", ...)` and the flag parsing block (lines 54-68) has no `--host-slug` flag and no `os.Getenv("HOST_SLUG")` call. The `DaemonConfig` struct in `daemon.go:37-48` also has no `HostSlug` field. A developer needs to know: (a) exactly where to add the flag/env read in `main.go`, (b) that `DaemonConfig` needs a new field, (c) the auto-select logic for zero/one/multiple hosts registered. The auto-select behavior ("if only one host is registered in `~/.mclaude/hosts/`, auto-select it") references on-disk state that the CLI manages — but the CLI `host register` command does not exist yet. The interaction between the on-disk scan and the NATS startup is not sequenced.
   - **Doc**: "If neither is set and only one host is registered in `~/.mclaude/hosts/`, auto-select it."
   - **Code**: `mclaude-session-agent/main.go:200` — `fmt.Sprintf("mclaude.users.%s.projects.%s.lifecycle._init", ...)` (no host); `daemon.go:37-48` — no `HostSlug` field in `DaemonConfig`; `main.go:54-68` — no `--host-slug` flag

5. **`daemon.go` references `mclaude-laptops` KV bucket — rename to `mclaude-hosts` is not specified atomically** — The ADR states "`mclaude-laptops` renamed to `mclaude-hosts`" and "`mclaude-laptops` KV bucket reference renamed to `mclaude-hosts` (hardcoded string in `daemon.go` updated)." The actual `daemon.go:85` opens `js.KeyValue(ctx, "mclaude-laptops")` and `daemon.go:56` names the field `laptopsKV`. However, `agent.go:28` uses a separate bucket `mclaude-heartbeats` (not `mclaude-laptops`). The spec-state-schema.md does not list `mclaude-heartbeats` at all — it is not in the bucket inventory. A developer needs to know: does `mclaude-heartbeats` become part of `mclaude-hosts`, get deleted, or stay unchanged? The ADR doesn't address this bucket.
   - **Doc**: "mclaude-laptops KV bucket reference renamed to mclaude-hosts (hardcoded string in daemon.go updated)"
   - **Code**: `agent.go:28` — `kvBucketHeartbeats = "mclaude-heartbeats"` (separate bucket not in schema); `daemon.go:85` — opens `mclaude-laptops`; `spec-state-schema.md` — `mclaude-heartbeats` not listed

6. **`handleJobsProjects` uses UUID-based KV prefix scan — ADR says switch to slug-based but does not specify the new filter** — The ADR states: "`handleJobsProjects` handler: KV key prefix lookup switches from UUID (`userID + '.'`) to slug-based (`userSlug + '.' + hostSlug + '.'`). This returns only the daemon's own host's projects." The current code at `daemon_jobs.go:887` uses `prefix := userID + "."`. The ADR says the new prefix is `userSlug + "." + hostSlug + "."`. But `DaemonConfig` has no `HostSlug` field (gap 4), and the function today has no access to userSlug or hostSlug (it uses `d.cfg.UserID`). Additionally, after BYOH, the KV key format is `{uslug}.{hslug}.{pslug}` — so the prefix `userSlug + "." + hostSlug + "."` would correctly scope to one host, but a developer must know what `ProjectState` field is used to filter further or whether the prefix alone is sufficient. The ADR says this is sufficient but does not say where `hostSlug` comes from in the handler (it must come from the new `DaemonConfig.HostSlug` field).
   - **Doc**: "KV key prefix lookup switches from UUID (`userID + '.'`) to slug-based (`userSlug + '.' + hostSlug + '.'`)"
   - **Code**: `daemon_jobs.go:887` — `prefix := userID + "."`; `DaemonConfig` has no `HostSlug` field

7. **`IssueHostJWT` function signature and NATS creds file format are underspecified for per-host use** — The ADR specifies a new `IssueHostJWT(accountNKey, hostPublicKey, userSlug, hostSlug)` function: "Unlike existing `IssueUserJWT`/`IssueSessionAgentJWT` which generate NKeys internally, this signs against a caller-supplied public key (the host's). Returns only the JWT — no seed." The return type is stated as `(string, error)`. However, the existing creds file format (`FormatNATSCredentials(jwt, seed)`) combines JWT and seed into a single `.creds` file. For host registration, the CLI generates the NKey locally, so it already has the seed. The developer must know: does `IssueHostJWT` return only the JWT (as stated), and does the CLI then assemble the `.creds` file itself by calling `FormatNATSCredentials(jwt, localSeed)`? And does `FormatNATSCredentials` need a public API export to the CLI, or does the CLI format the file itself? The ADR says the CLI "saves NATS creds at `~/.mclaude/hosts/{hslug}/nats.creds`" but does not specify whether the CLI calls `FormatNATSCredentials` or writes the file in its own format.
   - **Doc**: "Returns only the JWT — no seed." / "CLI saves NATS creds at `~/.mclaude/hosts/{hslug}/nats.creds`"
   - **Code**: `nkeys.go:149-164` — `FormatNATSCredentials(jwt, seed)` is the only existing helper; `IssueHostJWT` does not exist yet; CLI is a separate binary without shared library access to `mclaude-control-plane` internals

8. **`POST /api/users/{uslug}/hosts/code` response missing `serverUrl`** — The device-code flow step 3 says the web shows: `mclaude host register ABC123 --server https://mclaude.internal`. The `POST /api/hosts/register` success response includes `{slug, jwt, serverUrl}`. But `POST /api/users/{uslug}/hosts/code` response is specified as `{code: string, expiresAt: string (RFC3339)}` — it does not include `serverUrl`. The web UI already knows the server URL (it is talking to it), so it embeds it in the displayed command directly. However, the ADR does not explicitly state that `serverUrl` is inlined by the web UI from `window.location.origin` or a configured value — a developer implementing the web UI would need to know this. This is the only place where the source of `--server <url>` in the rendered CLI command is unspecified.
   - **Doc**: Step 3 of device-code flow: "Web shows: `mclaude host register ABC123 --server https://mclaude.internal`" / `POST /api/users/{uslug}/hosts/code` success: `{code: string, expiresAt: string}`
   - **Code**: No existing implementation; the schema gap is that `serverUrl` source for the web-rendered command is not defined.

## Run: 2026-04-20T12:00:00Z (Round 4)

**Gaps found: 5**

1. **Backfill Step 3 references a `projects.cluster_id` column that does not exist** — Step 3 of the migration DDL says "For each existing project row: Look up the project's cluster → find or create a host row for (user_id, cluster) → Set projects.host_id = host.id." But the ADR itself states "The current codebase has no `hosts`, `user_clusters`, or `host_id` on `projects` (ADR-0011 was never implemented)." If ADR-0011 was never implemented, `projects` has no `cluster_id` column — there is no cluster to look up. A developer implementing the backfill function cannot determine what the actual strategy is: create one default machine host per user and assign all their projects to it? Abort if no cluster is found? A concrete algorithm that works against the real schema is missing.
   - **Doc**: "Step 3: Backfill ... Look up the project's cluster → find or create a host row for (user_id, cluster)" vs. "The current codebase has no `hosts`, `user_clusters`, or `host_id` on `projects` (ADR-0011 was never implemented)."
   - **Code**: `mclaude-control-plane/db.go` — `projects` table has no `cluster_id` column; the referenced lookup cannot be performed.

2. **`serverUrl` in registration responses: source not specified** — Both `POST /api/users/{uslug}/hosts` (201: `{slug, jwt, serverUrl}`) and `POST /api/hosts/register` (201: `{slug, jwt, serverUrl}`) return a `serverUrl` field. The ADR never specifies how the control-plane determines this value — it is not a field in any request body, not derived from a stated env var, and not described as coming from a Helm value or `Host` header. A developer implementing these endpoints cannot determine what value to return.
   - **Doc**: Response schema at lines 363 and 368: `{slug: string, jwt: string, serverUrl: string}` — no specification of the value's origin.
   - **Code**: No existing implementation. No `SERVER_URL` env var referenced anywhere in the codebase.

3. **Admin cluster-member endpoint response codes unspecified** — `POST /admin/clusters/{cslug}/members` and `DELETE /admin/clusters/{cslug}/members/{uslug}` are listed without success HTTP status codes or error codes. The user flow section gives the POST request body (`{userSlug, role: "owner"|"user"}`), but neither endpoint has a response body schema, a success code (201? 200? 204?), or error codes (what code if the cluster slug is not found? if the user slug is not found? if the grant already exists for POST?). A developer implementing these handlers cannot proceed without this information.
   - **Doc**: "Admin cluster-member endpoints: `POST /admin/clusters/{cslug}/members` — grant user access (creates host row); `DELETE /admin/clusters/{cslug}/members/{uslug}` — revoke access (deletes host row + NATS revocation)" — no status codes, no response bodies, no error cases.
   - **Code**: No existing implementation in `mclaude-control-plane/admin.go`.

4. **`UserHostProjectEvents` and `UserHostProjectLifecycle` parameter type downgraded to raw `string`** — The new helper signatures are specified as `UserHostProjectEvents(u UserSlug, h HostSlug, p ProjectSlug, sslug string)` and `UserHostProjectLifecycle(u UserSlug, h HostSlug, p ProjectSlug, sslug string)`. The existing `UserProjectEvents` and `UserProjectLifecycle` use `s slug.SessionSlug` (the typed wrapper). Using a raw `string` for `sslug` breaks the compile-time type safety that is the central security and correctness guarantee of the ADR-0024 typed-slug scheme. A developer implementing these helpers would need to know whether to use `string` (as written) or `slug.SessionSlug` (as the type system requires).
   - **Doc**: Component Changes, `pkg/subj` section: `UserHostProjectEvents(u UserSlug, h HostSlug, p ProjectSlug, sslug string) string` and `UserHostProjectLifecycle(u UserSlug, h HostSlug, p ProjectSlug, sslug string) string`.
   - **Code**: `mclaude-common/pkg/subj/subj.go:116-127` — existing `UserProjectEvents` and `UserProjectLifecycle` both take `s slug.SessionSlug`, not `string`.

5. **Machine host presence: CP behavior on receiving `mclaude.users.{uslug}.hosts.{hslug}.status` is unspecified** — The spec-state-schema.md lists this subject with publisher=daemon (machine hosts) and subscriber=control-plane, SPA. The ADR says "Host presence: subscribe to `$SYS.ACCOUNT.*.CONNECT`/`DISCONNECT`, maintain in-memory map, expose via `GET /api/users/{uslug}/hosts` response." But `$SYS` events cover cluster leaf-node connections, not machine daemon connections. For machine hosts, the CP must derive online/offline status from something else. The `status` subject exists for this purpose — but the ADR never describes what the CP does when it receives a message on this subject (update the in-memory presence map? update `hosts.last_seen_at` in Postgres? update the `mclaude-hosts` KV entry?). The spec-state-schema.md says the daemon writes `mclaude-hosts` KV directly at startup + every 12h — so either CP reads KV to determine machine host status, or CP subscribes to the `status` subject. The two mechanisms are both mentioned but neither is designated as authoritative for machine host presence.
   - **Doc**: "Host presence: subscribe to `$SYS.ACCOUNT.*.CONNECT`/`DISCONNECT`, maintain in-memory map" (covers only `$SYS` events = cluster hosts). New subject `mclaude.users.{uslug}.hosts.{hslug}.status` subscriber listed as "control-plane, SPA" with no description of CP behavior on receipt.
   - **Code**: `spec-state-schema.md` — `mclaude-hosts` Writers: "daemon (`writeHostKV` — on startup + every 12h for machine hosts)"; Subject table — `mclaude.users.{uslug}.hosts.{hslug}.status` subscriber: "control-plane, SPA" — no described action.

## Run: 2026-04-20T17:28:55Z (Round 5)

**Gaps found: 2**

1. **Backfill Step 3 creates a `type='machine'` host row with `public_key=NULL`, violating the CHECK constraint** — The migration DDL Step 1 defines `CHECK (type = 'cluster' OR public_key IS NOT NULL)`, which means any `type='machine'` row must have a non-NULL `public_key`. But the Step 3 backfill comment says: "Create a default machine host row: slug='local', name='Local', type='machine', role='owner', `public_key=NULL` (will be populated on first daemon register)." This INSERT would fail immediately with a CHECK constraint violation. The two statements are directly contradictory within the same DDL section. A developer cannot implement the backfill as written — the constraint must either be relaxed during backfill (deferred, or using a different CHECK), or the backfill must omit the constraint and add it only after all rows are populated, or the backfill must insert with a placeholder public_key.
   - **Doc**: Migration DDL Step 1: `CHECK (type = 'cluster' OR public_key IS NOT NULL)` vs. Step 3 backfill: "type='machine', role='owner', `public_key=NULL` (will be populated on first daemon register)"
   - **Code**: The `hosts` table does not exist yet; the contradiction is internal to the ADR's own DDL section.

2. **`SessionsKVKey` KV helper signature uses raw `string` for session slug, inconsistent with all other typed helpers** — The ADR specifies `SessionsKVKey(u UserSlug, h HostSlug, p ProjectSlug, s string) string`. Every other slug parameter in every `pkg/subj` helper — including the new `UserHostProjectEvents(u UserSlug, h HostSlug, p ProjectSlug, s SessionSlug)` and `UserHostProjectLifecycle(u UserSlug, h HostSlug, p ProjectSlug, s SessionSlug)` — uses typed wrappers. The existing `SessionsKVKey(u UserSlug, p ProjectSlug, s slug.SessionSlug) string` uses `slug.SessionSlug`. The ADR-0024 typed-slug scheme's core guarantee is compile-time type safety for all slug tokens. Downgrading the session parameter in the KV key helper to `string` means callers can pass any unvalidated string as the session component of the key — the exact attack surface the scheme is designed to prevent. A developer implementing `SessionsKVKey` must decide whether to follow the written spec (`string`) or the established type-safe pattern (`SessionSlug`).
   - **Doc**: Component Changes, `pkg/subj` KV key helpers: `SessionsKVKey(u UserSlug, h HostSlug, p ProjectSlug, s string) string`
   - **Code**: `mclaude-common/pkg/subj/subj.go:158` — existing `SessionsKVKey(u slug.UserSlug, p slug.ProjectSlug, s slug.SessionSlug) string` uses `SessionSlug`; `subj.go:116` — `UserProjectEvents` uses `s slug.SessionSlug`, the pattern the new helpers follow.
