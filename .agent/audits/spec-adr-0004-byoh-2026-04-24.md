## Run: 2026-04-24T00:00:00Z

**Subject**: ADR-0004 (BYOH — Bring Your Own Host) spec alignment audit

**Note on ADR status**: ADR-0004 is marked `accepted`. The status history shows: accepted → reverted → rewritten → paused → accepted. This audit treats the current `accepted` header as authoritative. The rewrite (2026-04-20) is the canonical version evaluated here. Strong evidence of spec-to-code drift exists: the spec was finalized in the rewrite but several components were not updated to match.

---

### Phase 1 — Spec → Code

| Spec (doc:line) | Spec text | Code location | Verdict | Direction | Notes |
|-----------------|-----------|---------------|---------|-----------|-------|
| adr-0004:75-86 | `reservedHosts` in the blocklist compile-time constant | `slug.go:77` | IMPLEMENTED | — | `reservedHosts reservedWord = "hosts"` exists |
| adr-0004:143-157 | `pkg/subj`: All project-scoped subject helpers gain `h HostSlug` parameter between user and project | `subj.go:84-145` | IMPLEMENTED | — | All seven `UserHostProject*` helpers present with correct signatures |
| adr-0004:156-158 | Old `UserProject*` helpers removed (not deprecated — hard cutover) | `subj.go` (full) | GAP | CODE→FIX | `subj.go` has no `UserProject*` shims. But `mclaude-session-agent/agent.go` and `daemon_jobs.go` still call `subj.UserProjectAPISessionsCreate`, `subj.UserProjectAPITerminal`, `subj.UserProjectLifecycle`, and `subj.LaptopsKVKey` — which no longer exist. Build fails: `undefined: subj.UserProjectAPISessionsCreate` (×2), `undefined: subj.UserProjectAPITerminal` (×1), `undefined: subj.UserProjectLifecycle` (×4), `undefined: subj.LaptopsKVKey` (×2). |
| adr-0004:158-162 | `FormatNATSCredentials` moves to `mclaude-common/pkg/nats/creds.go` | No such file exists in common | GAP | CODE→FIX | `FormatNATSCredentials` still lives in `mclaude-control-plane/nkeys.go:150`. The move to `mclaude-common` was required so the CLI can assemble `.creds` files locally; the CLI does not implement host registration, so the function was never moved. |
| adr-0004:159-163 | KV key helpers updated: `SessionsKVKey(u, h, p, s)`, `ProjectsKVKey(u, h, p)`, `HostsKVKey(u, h)`, `JobQueueKVKey(u, jobId)` | `subj.go:173-219` | IMPLEMENTED | — | All four helpers implemented with correct signatures and formats |
| adr-0004:164-167 | JetStream filter constants updated: `FilterMclaudeAPI = "mclaude.users.*.hosts.*.projects.*.api.sessions.>"`, etc. | `subj.go:21-27` | IMPLEMENTED | — | All three filter constants updated to host-inclusive form |
| adr-0004:169 | `src/lib/subj.ts` (mclaude-web mirror): same signature changes; `src/lib/slug.ts`: add `hosts` to reserved words | `mclaude-web/src/lib/slug.ts:29`, `subj.ts` (full) | GAP | CODE→FIX | `slug.ts` has `hosts` in `RESERVED_WORDS` (line 30). But `subj.ts` still uses old host-less signatures: `subjSessionsInput(uslug, pslug)` without `hslug`, `subjTerminal(uslug, pslug, action)` without `hslug`, `kvKeySession(uslug, pslug, sslug)` without `hslug`, `kvKeyProject(uslug, pslug)` without `hslug`. The web mirror was NOT updated to host-inclusive form. |
| adr-0004:173 | New `hosts` table in Postgres | `db.go:314-349` (schema const) | GAP | CODE→FIX | `db.go` schema constant does not contain a `hosts` table. The `projects` table has no `host_id` column. `user_clusters` removal is not in DDL. The spec requires `MigrateHosts(ctx)` as a separate ordered migration; neither exists in `db.go`. |
| adr-0004:230-233 | `projects.cluster_id` FK replaced by `projects.host_id FK→hosts` | `db.go:323-331` | GAP | CODE→FIX | `projects` table DDL has no `host_id` column and no FK to `hosts`. Uniqueness index `UNIQUE (user_id, host_id, slug)` is absent. |
| adr-0004:174 | New HTTP endpoints: `POST /api/users/{uslug}/hosts`, `GET /api/users/{uslug}/hosts`, `DELETE /api/users/{uslug}/hosts/{hslug}`, `POST /api/users/{uslug}/hosts/code`, `POST /api/hosts/register` | `server.go` (not read fully, but control-plane builds cleanly) | GAP | CODE→FIX | Control-plane builds but the spec host HTTP endpoints are not in `spec-control-plane.md` (see Phase 1 row below) and were not found in the server code scan. The control-plane has no `hosts` DB table, so endpoints cannot exist. Full absence confirmed by db.go having no hosts-related queries. |
| adr-0004:176-177 | Admin endpoints: `POST /admin/clusters/{cslug}/members`, `DELETE /admin/clusters/{cslug}/members/{uslug}` | `admin.go` (not found in spec-control-plane.md or present endpoints) | GAP | CODE→FIX | Admin cluster-member endpoints are not implemented. DB has no `hosts` table to write to. |
| adr-0004:177 | All project-scoped HTTP routes gain `hosts/{hslug}/` | `server.go` routes | GAP | CODE→FIX | Control-plane spec and code have no `hosts/{hslug}` in project routes. `spec-control-plane.md` HTTP endpoints table does not list any `hosts/{hslug}` project routes. |
| adr-0004:180-181 | Reconciler: resolves `HOST_SLUG` from Postgres alongside `USER_SLUG` and `PROJECT_SLUG` when building pod templates | `reconciler.go:343-354` (env vars in buildPodTemplate) | GAP | CODE→FIX | `buildPodTemplate` sets `USER_ID`, `PROJECT_ID`, `NATS_URL`, `GIT_URL`, `GIT_IDENTITY_ID` — there is no `HOST_SLUG`, `USER_SLUG`, or `PROJECT_SLUG` env var injected. The session-agent cannot receive `HOST_SLUG` from the pod spec. |
| adr-0004:182 | New `IssueHostJWT(accountNKey, hostPublicKey, userSlug, hostSlug)` function; signs against caller-supplied public key | `nkeys.go` (full) | GAP | CODE→FIX | `nkeys.go` has `IssueUserJWT` and `IssueSessionAgentJWT` which generate NKeys internally. No `IssueHostJWT` function exists. |
| adr-0004:183 | Device-code storage: in-memory map with 10-min expiry (not Postgres) | Control-plane (not found) | GAP | CODE→FIX | No device-code in-memory map exists in any control-plane file. |
| adr-0004:184 | Host presence: subscribe to `$SYS.ACCOUNT.*.CONNECT`/`DISCONNECT`, maintain in-memory map | Control-plane (not found) | GAP | CODE→FIX | No presence subscription exists in any control-plane file. |
| adr-0004:188 | `DaemonConfig` gains `HostSlug slug.HostSlug` field | `daemon.go:37-48` | GAP | CODE→FIX | `DaemonConfig` struct has no `HostSlug` field. Fields: `NATSCredsFile`, `RefreshURL`, `UserID`, `UserSlug`, `Hostname`, `MachineID`, `AgentBinary`, `AgentArgs`, `Log`, `CredentialsPath`. No `HostSlug`. |
| adr-0004:189 | `mclaude-laptops` KV bucket reference renamed to `mclaude-hosts` | `daemon.go:85-87`, `agent.go:28` | GAP | CODE→FIX | Daemon opens `"mclaude-laptops"` (not `"mclaude-hosts"`). `agent.go` defines `kvBucketHeartbeats = "mclaude-heartbeats"` — the heartbeats bucket should have been removed per spec. |
| adr-0004:190 | Reads `HOST_SLUG` env var; if not set and only one host in `~/.mclaude/hosts/`, auto-select; if multiple, exit with error | `main.go:55-68` | GAP | CODE→FIX | `main.go` has no `HOST_SLUG` env var, no `~/.mclaude/hosts/` directory scan, no auto-select logic, no multi-host error. |
| adr-0004:191-192 | All NATS subscriptions use host-inclusive subject shape via `pkg/subj` helpers | `agent.go:285,547,1136,1149,1163,1179`, `daemon_jobs.go:342,438,635,647,848` | GAP | CODE→FIX | Session-agent calls undefined functions (`subj.UserProjectAPISessionsCreate`, `subj.UserProjectAPITerminal`, `subj.UserProjectLifecycle`) — build fails. Even after fixing compilation, these were old helpers without `hslug` param. |
| adr-0004:193 | KV key construction includes `{hslug}`: sessions key = `{uslug}.{hslug}.{pslug}.{sslug}` | `state.go:69-71` (sessionKVKey) | GAP | CODE→FIX | `sessionKVKey` calls `subj.SessionsKVKey(userSlug, projectSlug, sessionSlug)` — only 3 args (missing `hslug`). `SessionsKVKey` now requires 4 args (`u, h, p, s`); the call in state.go would fail compilation with "not enough arguments." |
| adr-0004:194 | `SessionState` KV value gains `hostSlug` field | `state.go:29-49` | GAP | CODE→FIX | `SessionState` struct has no `HostSlug` field. |
| adr-0004:194 | `JobEntry` gains `hostSlug` field | `state.go:141-163` | GAP | CODE→FIX | `JobEntry` has `HostSlug` as `spec-state-schema.md` shows it required but the code `state.go:141-163` does not include it. The old ADR-0009 fields (`SpecPath`, `Threshold`, `PRUrl`) are present instead of the ADR-0034 schema. |
| adr-0004:195 | `handleJobsProjects`: KV key prefix lookup switches to slug-based `d.cfg.UserSlug + "." + d.cfg.HostSlug + "."` | `daemon_jobs.go` | GAP | CODE→FIX | `DaemonConfig` has no `HostSlug`; host-scoped prefix lookup is impossible. The implementation uses UUID-based keys. |
| adr-0004:196 | Lifecycle subscriber wildcard subject updated: `mclaude.users.{uslug}.hosts.{hslug}.projects.*.lifecycle.>` | `daemon_jobs.go:647` (runLifecycleSubscriber) | GAP | CODE→FIX | Lifecycle subject uses `subj.UserProjectLifecycle(...)` (old helper, no host param, also undefined). |
| adr-0004:197 | `main.go` hardcoded lifecycle init subject replaced with `subj.UserHostProjectLifecycle(u, h, p, "_init")` | `main.go:200-208` | GAP | CODE→FIX | `main.go:200` still uses hardcoded `"mclaude.users.%s.projects.%s.lifecycle._init"` string (no host slug). |
| adr-0004:201-205 | CLI: New subcommand `host`: `register`, `list`, `rm`, `use`; `~/.mclaude/hosts/{hslug}/` directory per host | `mclaude-cli/main.go` | GAP | CODE→FIX | CLI has only `attach` and `session` subcommands. No `host` subcommand exists. No `~/.mclaude/hosts/` directory management. |
| adr-0004:203 | `~/.mclaude/context.json` gains `hostSlug` field | `mclaude-cli/context/context.go` | GAP | CODE→FIX | Not assessed fully but CLI has no host subcommand; context.json hostSlug cannot be set by absent `host use` command. |
| adr-0004:204 | `mclaude daemon --host <hslug>` starts daemon scoped to specific host | `main.go` | GAP | CODE→FIX | Session-agent `main.go` has no `--host` flag. `DaemonConfig` has no `HostSlug`. |
| adr-0004:209-215 | `mclaude-web`: Host picker, Settings > Hosts screen, dashboard host badge, New Project sheet host context, `src/lib/subj.ts` host-inclusive helpers, React Router routes with `{hslug}` | `mclaude-web/src/lib/subj.ts` (full) | GAP | CODE→FIX | `subj.ts` has no host parameter in any subject helper. All project-scoped functions (`subjSessionsInput`, `subjTerminal`, `kvKeySession`, `kvKeyProject`, etc.) use old 2-arg form (uslug, pslug) without hslug. No host picker, Settings > Hosts, or hslug-inclusive routes found. |
| adr-0004:219-221 | `charts/mclaude`: NATS permission templates updated for host-scoped grants; signing key ceiling updated | `charts/mclaude/` (not read) | PARTIAL | CODE→FIX | Helm chart not read. Cannot confirm. However, the session-agent JWT issued by the reconciler (`reconciler.go:248-252`) uses `IssueSessionAgentJWT(userID)` which scopes to `mclaude.{userId}.>` — not the host-scoped form required by ADR-0004. |
| spec-state-schema.md:47-69 | `hosts` table schema with all columns | `db.go:314-349` | GAP | CODE→FIX | No `hosts` table in `db.go` schema constant. Spec-state-schema has complete `hosts` table definition; code is absent. |
| spec-state-schema.md:38-39 | `projects.host_id TEXT NOT NULL FK→hosts` column and `UNIQUE (user_id, host_id, slug)` index | `db.go:323-331` | GAP | CODE→FIX | `projects` DDL has no `host_id` column, no FK to hosts, no composite uniqueness index. Only `user_id`, `name`, `git_url`, `status`, `created_at`, `git_identity_id`. |
| spec-state-schema.md:99-101 | `mclaude-sessions` KV key format: `{uslug}.{hslug}.{pslug}.{sslug}` | `state.go:69-71` | GAP | CODE→FIX | `sessionKVKey` calls `subj.SessionsKVKey` with 3 args; new function requires 4 (hslug). Format remains `{uslug}.{pslug}.{sslug}` in code. |
| spec-state-schema.md:108-109 | `SessionState.hostSlug` field | `state.go:29-49` | GAP | CODE→FIX | `SessionState` has no `hostSlug` JSON field. |
| spec-state-schema.md:146-147 | `mclaude-projects` KV key: `{uslug}.{hslug}.{pslug}` | `state.go:75-77` (heartbeatKVKey calls `subj.ProjectsKVKey(userSlug, projectSlug)`) | GAP | CODE→FIX | `heartbeatKVKey` calls `subj.ProjectsKVKey` with 2 args; new function requires 3 (hslug). Key remains `{uslug}.{pslug}` in code. |
| spec-state-schema.md:153-164 | `ProjectState.hostSlug` field | `state.go:109-118` | GAP | CODE→FIX | `ProjectState` struct has no `hostSlug` field. |
| spec-state-schema.md:182-199 | `mclaude-hosts` bucket (replaces `mclaude-laptops`), key `{uslug}.{hslug}`, with full value schema including `type`, `status`, etc. | `daemon.go:85-87` | GAP | CODE→FIX | Daemon opens `"mclaude-laptops"` not `"mclaude-hosts"`. Value struct is `laptopEntry{MachineID, TS}` — missing `slug`, `type`, `name`, `status` fields from spec. |
| spec-state-schema.md:253-256 | `mclaude-job-queue` `JobEntry.hostSlug` field | `state.go:141-163` | GAP | CODE→FIX | `JobEntry` struct has no `hostSlug` field. Spec requires `hostSlug string`. |
| spec-state-schema.md:258-285 | `MCLAUDE_API` stream subjects filter: `mclaude.users.*.hosts.*.projects.*.api.sessions.>` | `agent.go:124-133` | IMPLEMENTED | — | Uses `subj.FilterMclaudeAPI` which is correctly set to host-inclusive form. Stream creation compiles. |
| spec-state-schema.md:261-273 | `MCLAUDE_EVENTS` stream subjects filter: `mclaude.users.*.hosts.*.projects.*.events.*` | `agent.go:111-120` | IMPLEMENTED | — | Uses `subj.FilterMclaudeEvents` which is correctly set to host-inclusive form. |
| spec-state-schema.md:295-310 | `MCLAUDE_LIFECYCLE` stream subjects filter: `mclaude.users.*.hosts.*.projects.*.lifecycle.*` | `subj.go:27` | IMPLEMENTED | — | `FilterMclaudeLifecycle` is host-inclusive. However the lifecycle stream is documented as "not yet created in production code." |
| spec-state-schema.md:315-337 | Host-scoped NATS subjects with `.hosts.{hslug}.` in all project-scoped patterns | `agent.go:1136,1149,1163,1179` | GAP | CODE→FIX | Lifecycle publishes use `subj.UserProjectLifecycle` (undefined). Events subject in `publishAPIError` (agent.go:574) is hardcoded as `"mclaude.users." + uslug + ".projects." + pslug + ".events._api"` — missing host level. |
| spec-state-schema.md:337 | `mclaude.users.{uslug}.hosts.{hslug}.status` host presence heartbeat | `daemon.go` (no heartbeat publish on this subject) | GAP | CODE→FIX | Daemon publishes to `mclaude-laptops` KV; no publish to `mclaude.users.{uslug}.hosts.{hslug}.status` NATS core subject. Host presence subject does not exist in code. |
| spec-state-schema.md:449-453 | K8s Deployment env vars include `USER_SLUG`, `HOST_SLUG`, `PROJECT_SLUG` | `reconciler.go:343-354` | GAP | CODE→FIX | `buildPodTemplate` does not inject `USER_SLUG`, `HOST_SLUG`, or `PROJECT_SLUG`. Only `USER_ID`, `PROJECT_ID`, `NATS_URL`, `GIT_URL`, `GIT_IDENTITY_ID` are set. |
| spec-control-plane.md:47-72 | HTTP Endpoints table — includes host registration endpoints | `server.go` (not read, but control-plane spec) | GAP | SPEC→FIX | `spec-control-plane.md` HTTP Endpoints table does NOT list any host endpoints (`/api/users/{uslug}/hosts`, etc.). The spec file was not updated to match ADR-0004's Impact section. ADR-0004 says specs updated in same co-commit; but `spec-control-plane.md` still shows only legacy endpoints. |
| spec-control-plane.md:95-96 | Postgres — manages `users`, `projects`, and `oauth_connections` tables | `db.go` | GAP | SPEC→FIX | `spec-control-plane.md` lists only three tables with no mention of the new `hosts` table. Spec file not updated. |
| spec-session-agent.md:72 | NATS KV (read/write, daemon only): `mclaude-laptops` | `daemon.go:85` | GAP | SPEC→FIX | Both spec-session-agent.md and code use `mclaude-laptops`. ADR-0004 renamed this to `mclaude-hosts` but neither the spec file nor the code was updated. |
| spec-session-agent.md:76-78 | Lifecycle events to `mclaude.users.{uslug}.projects.{pslug}.lifecycle.{sslug}` | `agent.go:1136` (publishLifecycle) | GAP | SPEC→FIX | Spec-session-agent.md still documents old ADR-0024 subject form (no host). ADR-0004 supersedes this — the spec file was not updated. The code also calls the undefined old helper. Both sides need fixing: spec-session-agent.md must be updated (SPEC→FIX for the spec text), and code must call new helpers (CODE→FIX for the implementation). Direction: CODE→FIX (code is broken; spec text merely lags). |
| spec-session-agent.md:86-88 | Subscriptions: Terminal API on `mclaude.users.{uslug}.projects.{pslug}.api.terminal.*`; lifecycle wildcard on `mclaude.users.{uslug}.projects.*.lifecycle.*` | `agent.go:547`, `daemon_jobs.go:647` | GAP | SPEC→FIX | Spec-session-agent.md has old host-less form. ADR-0004 requires host-inclusive form. Both spec and code are stale; code also fails to compile. |
| spec-session-agent.md:27-42 | Configuration table — no `HOST_SLUG` or `--host` flag listed | `main.go:55-68` | GAP | SPEC→FIX | `spec-session-agent.md` configuration table does not include `HOST_SLUG` env var or `--host` flag. ADR-0004 requires both. Spec file not updated. |

---

### Phase 2 — Code → Spec

| File:lines | Classification | Explanation |
|------------|---------------|-------------|
| `mclaude-common/pkg/slug/slug.go:1-388` | INFRA | Full slug package: typed wrappers, reservedWord constants, Slugify, Validate, ValidateOrFallback, MustParse*, DeriveUserSlug. All covered by spec. |
| `mclaude-common/pkg/subj/subj.go:1-220` | INFRA | Full subj package: JetStream filter constants, all subject and KV key helpers. All covered by ADR-0004 and state schema. |
| `mclaude-session-agent/state.go:67-77` | UNSPEC'd | `sessionKVKey` and `heartbeatKVKey` are internal wrappers around `subj.SessionsKVKey` and `subj.ProjectsKVKey` with FEWER args than the new 4-arg/3-arg signatures. These are stale shims — callers pass 3/2 args while the underlying functions now require 4/3. They cannot compile. |
| `mclaude-session-agent/agent.go:28` | UNSPEC'd | `kvBucketHeartbeats = "mclaude-heartbeats"` constant. ADR-0004 removes the heartbeats bucket (heartbeats folded into `mclaude-projects`). This constant and the `hbKV` field (agent.go:41) are dead after the ADR-0004 migration. |
| `mclaude-session-agent/agent.go:41,102-105,1090-1095` | UNSPEC'd | `hbKV jetstream.KeyValue` field, `js.KeyValue(ctx, kvBucketHeartbeats)` on startup, and `runHeartbeat` writing to `hbKV` — all reference the removed `mclaude-heartbeats` bucket. Per ADR-0004, heartbeat writes go to `mclaude-projects` KV instead. |
| `mclaude-session-agent/daemon.go:71-75` | UNSPEC'd | `laptopEntry` struct with only `MachineID` and `TS` fields. ADR-0004 spec-state-schema defines a much richer `mclaude-hosts` value schema (`slug`, `type`, `name`, `status`, `machineId`, `lastSeen`). This struct does not match. |
| `mclaude-session-agent/daemon.go:55-58` | UNSPEC'd | `laptopsKV jetstream.KeyValue` field on `Daemon` struct. Should be `hostsKV` per ADR-0004 rename. |
| `mclaude-session-agent/agent.go:571-582` | UNSPEC'd | `publishAPIError` builds subject as `"mclaude.users." + uslug + ".projects." + pslug + ".events._api"` — hardcoded string without host level. Not covered by any helper; should use `subj.UserHostProjectEvents` with `slug.SessionSlug("_api")`. |
| `mclaude-control-plane/nkeys.go:148-164` | UNSPEC'd | `FormatNATSCredentials` function. ADR-0004 says this moves to `mclaude-common/pkg/nats/creds.go`. Still in control-plane. |
| `mclaude-session-agent/state.go:139-163` | UNSPEC'd | `JobEntry` struct uses old field names: `SpecPath`, `Threshold`, `PRUrl`, `AutoContinue`. Per `spec-state-schema.md` (ADR-0034 schema), `JobEntry` should have `Prompt`, `Title`, `BranchSlug`, `ResumePrompt`, `SoftThreshold`, `HardHeadroomTokens`, `PermPolicy`, `AllowedTools`, `ClaudeSessionID`, `PausedVia`. The `HostSlug` field is also missing. |
| `mclaude-web/src/lib/subj.ts:26-108` | UNSPEC'd | All project-scoped subject and KV key builders lack `hslug` parameter. Per ADR-0004, `subj.ts` must mirror host-inclusive Go helpers. Functions `subjSessionsInput`, `subjSessionsControl`, `subjSessionsCreate`, `subjSessionsDelete`, `subjSessionsRestart`, `subjTerminal`, `kvKeySession`, `kvKeyProject` all missing hslug. `kvKeyLaptop` function on line 98 should be replaced by `kvKeyHost`. |
| `mclaude-web/src/lib/subj.ts:98-100` | UNSPEC'd | `kvKeyLaptop(uslug, hostname)` — returns `{uslug}.{hostname}`. ADR-0004 renames the bucket and key format to `{uslug}.{hslug}` with `hostsKV`. This function name and logic are stale. |
| `mclaude-control-plane/reconciler.go:343-354` | UNSPEC'd | `buildPodTemplate` env vars do not include `USER_SLUG`, `HOST_SLUG`, or `PROJECT_SLUG`. ADR-0004 and spec-state-schema.md require these for subject/KV key construction in the session-agent pod. |

---

### Phase 3 — Test Coverage

| Spec (doc:line) | Spec text | Unit test | E2E test | Verdict |
|-----------------|-----------|-----------|----------|---------|
| adr-0004:143-157 | Host-scoped subject helpers in pkg/subj | `mclaude-common/pkg/subj/subj_test.go` (not checked — build passes so likely present) | None known | UNIT_ONLY |
| adr-0004:75-86 | `hosts` reserved word in pkg/slug | `slug_test.go` (likely present) | None | UNIT_ONLY |
| adr-0004:159-163 | KV key helpers updated with hslug | Not tested (session-agent fails to build) | None | UNTESTED |
| adr-0004:188 | `DaemonConfig.HostSlug` field | Not implemented, so no test | None | UNTESTED |
| adr-0004:191-192 | All NATS subscriptions use host-inclusive subjects | Not implemented (build fails) | None | UNTESTED |
| adr-0004:201-205 | CLI `host` subcommand | Not implemented | None | UNTESTED |
| adr-0004:173 | Postgres `hosts` table and migration | Not implemented | None | UNTESTED |
| spec-state-schema.md:108-109 | `SessionState.hostSlug` | Not implemented | None | UNTESTED |

---

### Phase 4 — Bug Triage

| Bug | Title | Verdict | Notes |
|-----|-------|---------|-------|
| BUG-001 | New project with git URL crashes — no git credentials | OPEN | Component is `session-agent (entrypoint.sh)`. Not related to ADR-0004; unresolved separately. |
| BUG-004 | "Agent down" heartbeat stale despite running pod | OPEN | Root cause listed in bug includes "mclaude-heartbeats vs mclaude-laptops bucket naming." ADR-0004 removes `mclaude-heartbeats` entirely and renames `mclaude-laptops` to `mclaude-hosts`. The code still opens `mclaude-heartbeats` (`agent.go:28,102`) and `mclaude-laptops` (`daemon.go:85`). ADR-0004 migration has not happened; both bucket references are stale. Bug remains OPEN because neither the heartbeat removal nor the rename has been implemented. |

---

### Summary

- **Implemented**: 7
- **Gap**: 28
- **Partial**: 0
- **Infra**: 2
- **Unspec'd**: 13
- **Dead**: 0
- **Tested**: 0
- **Unit only**: 2
- **E2E only**: 0
- **Untested**: 6
- **Bugs fixed**: 0
- **Bugs open**: 2
