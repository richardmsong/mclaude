## Run: 2026-04-20T00:00:00Z

**Gaps found: 9**

VERDICT: BLOCKING

---

1. **Subject inventory is incomplete vs. spec-state-schema.md** — The ADR's "NATS subject inventory" table lists 12 old→new subject mappings, but `spec-state-schema.md` contains additional subjects not covered by the ADR. Specifically, `spec-state-schema.md` lists `mclaude.clusters.{clusterId}.status` (published by worker controller to control-plane) and the ADR maps it to `mclaude.clusters.{cslug}.api.status`. However, `spec-state-schema.md` also contains a `mclaude-clusters` KV bucket (key: `{userId}`, value: per-user cluster list) mentioned in ADR-0016's KV table that is absent from the ADR-0024 KV key format table entirely — no old→new mapping is provided. A developer cannot tell whether this bucket's key format changes.
   - **Doc**: "KV key format — Bucket | Old key | New key" table only lists `mclaude-sessions`, `mclaude-projects`, `mclaude-laptops`, `mclaude-job-queue`.
   - **Schema**: ADR-0016 (NATS Security) lists a `mclaude-clusters` KV bucket with key `{userId}` and writer/reader assignments. `spec-state-schema.md` also references it.

2. **ADR-0016 subject-permission grants use the old subject shape — no update specified** — ADR-0016 is listed as a downstream ADR that "gets a mechanical subject-string update," but the subject permission grants in ADR-0016 use both the old positional shape (`mclaude.{userId}.>` for SPA, `mclaude.{userId}.sessions.{clusterId}.{sessionId}.>` for session-agent) and the signing key ceiling (`mclaude.*.sessions.{clusterId}.*.>`). After the typed-slug migration, these permission strings must change to the new shape (`mclaude.users.{uslug}.>`, `mclaude.users.{uslug}.projects.{pslug}.api.*`, etc.), but the ADR-0024 document does not specify what the updated NATS JWT permission grant strings will be. A Helm chart developer cannot update the NATS account permission templates without knowing the exact new permission strings.
   - **Doc**: "NATS account permission templates switch to the new subject shape. Grants use `mclaude.users.{uslug}.>` for per-user scope and `mclaude.clusters.{cslug}.>` for cluster-scope."
   - **ADR-0016**: Session-agent JWT ceiling is `mclaude.*.sessions.{clusterId}.*.>`. The new equivalent is unspecified. The SPA deny list (`$KV.>`, `$JS.>`, `mclaude.system.>`) is also unspecified in the new scheme.

3. **Session-agent NATS subject shape contradiction with ADR-0016** — ADR-0016 specifies that session-agent NATS JWT permissions are scoped to `mclaude.{userId}.sessions.{clusterId}.{sessionId}.>`. This subject shape does not appear in the ADR-0024 subject inventory at all — session-agent subjects in ADR-0024 follow the new `mclaude.users.{uslug}.projects.{pslug}.api.sessions.*` shape. The ADR does not reconcile how session-agent JWT issuance (which is driven by the controller signing key ceiling, not the control-plane) transitions to the new scheme. A developer implementing the session-agent subscription rewrites cannot determine the correct NATS permission scope for the new JWT.
   - **Doc**: "Subscriptions switch to the new subject shape via `pkg/subj`" (mclaude-session-agent section). No mention of JWT ceiling update.
   - **ADR-0016**: "ceiling: `mclaude.*.sessions.{clusterId}.*.>`" for the scoped signing key — this is a different structural pattern than the new `mclaude.users.{uslug}.projects.{pslug}.*` tree.

4. **`mclaude-job-queue` key separator change breaks ADR-0009 callers** — ADR-0009 (Quota-Aware Scheduling) specifies `mclaude-job-queue` keys as `{userId}/{jobId}` (forward-slash separator) in multiple places: the KV bucket key format definition, `runJobDispatcher` code (`d.jobQueueKV` keyed `{d.cfg.UserID}/{jobId}`), and the HTTP server scope comment. ADR-0024 changes the separator to a dot: `{uslug}.{jobId}`. ADR-0024 notes this change but does not specify whether `jobId` remains a UUID or becomes a slug, nor does it specify how existing `mclaude-job-queue` entries (with slash-separated UUID keys) are migrated at cutover. A developer implementing the hard cutover cannot determine whether old entries are purged, re-keyed, or abandoned.
   - **Doc**: "`mclaude-job-queue` renames from `{uid}/{jobId}` to `{uslug}.{jobId}`" and "`{jobId}` and `{hostname}` remain UUID/opaque tokens (not slugs) — no change."
   - **ADR-0009**: Key format `{userId}/{jobId}` is used throughout including in Go struct comments and handler logic. No migration path for existing KV entries is mentioned in ADR-0024.

5. **HTTP URL inventory is incomplete — no full endpoint listing for moved routes** — The HTTP URL table shows old→new path patterns but does not list all HTTP endpoints that move. For example, the SPA currently calls project-creation via NATS (`mclaude.{userId}.api.projects.create`), but `GET /api/projects`, `POST /api/projects`, `GET /api/projects/{id}/sessions`, etc. are implicit. The table shows `/api/projects/*` → `/api/users/{uslug}/projects/*` and `/api/jobs/*` → `/api/users/{uslug}/jobs/*`, but does not enumerate which specific method+path combinations exist, whether they are request/reply NATS subjects or HTTP REST endpoints, or what the full new path is. Developers cannot implement handler restructuring without a complete route list.
   - **Doc**: "HTTP URL inventory" table shows 6 rows, all at the pattern level, none at the method+path level with request/response payloads.
   - **Code prerequisite**: ADR-0011 specifies `POST /admin/clusters/{id}/members`, `DELETE /admin/clusters/{id}/members/{userId}`, etc. using `{id}` (UUID), not `{cslug}`. The ADR-0024 admin subtree says `/admin/users/{uslug}/...` but leaves cluster admin routes unaddressed.

6. **`users.slug` backfill collision handling is underspecified** — The migration SQL in the Data Model section shows a simplified `UPDATE users SET slug = regexp_replace(...) || '-' || split_part(email, '@', 2)` and notes "real migration uses the slugify() helper in a plpgsql function + numeric suffix on collisions." No such plpgsql function is defined in the ADR. A developer writing the database migration cannot implement collision-safe backfill without this definition, yet the ADR explicitly defers it to the "real migration." The same ambiguity applies to the `projects` and `clusters` backfill (shown as `<slugify(name) + collision suffix>` pseudocode).
   - **Doc**: "^ simplified — real migration uses the slugify() helper in a plpgsql function + numeric suffix on collisions" and `UPDATE projects SET slug = <slugify(name) + collision suffix>`.
   - **Code**: No plpgsql function definition exists in the spec. The Go `slugify()` algorithm is defined in the Decisions table, but its translation to SQL for the backfill migration is not.

7. **`mclaude-sessions` KV key in ADR-0009 daemon code still uses `{userId}.{projectId}.{sessionId}` with IDs, not slugs** — ADR-0009 specifies the `runJobDispatcher` polls `d.sessKV` using key format `{userId}.{projectId}.{sessionID}` (matching `sessionKVKey` in state.go, using UUIDs). After the slug migration, the key becomes `{uslug}.{pslug}.{sslug}`. ADR-0009's `runJobDispatcher` stores `job.ProjectID` (a UUID), `job.UserID` (a UUID), and `job.SessionID` (a session UUID) — none of which are slugs. The ADR-0024 does not specify how the daemon dispatches sessions after cutover: specifically whether `JobEntry` gains slug fields alongside UUID fields, or whether the daemon must resolve UUID→slug before constructing KV keys.
   - **Doc**: ADR-0024 says "KV key format changes from `{userId}.{projectId}.{sessionId}` to `{uslug}.{pslug}.{sslug}`" for `mclaude-sessions`.
   - **ADR-0009**: `runJobDispatcher` uses `job.ProjectID`, `job.UserID`, `job.SessionID` (all UUIDs) to poll `d.sessKV`. `JobEntry` struct has no slug fields.

8. **JetStream stream subject filters are not updated** — The ADR states "JetStream streams (`MCLAUDE_API`, `MCLAUDE_EVENTS`, `MCLAUDE_LIFECYCLE`) stay by name; their subject filters update to the new patterns." The current subject filters in `spec-state-schema.md` are `mclaude.*.*.events.*`, `mclaude.*.*.api.sessions.>`, and `mclaude.*.*.lifecycle.*` (3-token wildcard prefix). The new typed subjects have 5 tokens before the semantic suffix (e.g., `mclaude.users.{uslug}.projects.{pslug}.events.{sslug}`). The ADR does not specify what the new subject filter patterns will be. A developer implementing the stream configuration update cannot determine the correct filter strings.
   - **Doc**: "their subject filters update to the new patterns" — new patterns are not stated.
   - **Schema**: `MCLAUDE_EVENTS` filter is `mclaude.*.*.events.*`; `MCLAUDE_API` is `mclaude.*.*.api.sessions.>`; `MCLAUDE_LIFECYCLE` is `mclaude.*.*.lifecycle.*`.

9. **`mclaude-projects` KV key change is ambiguous for ADR-0011 multi-cluster** — ADR-0011 specifies that the `mclaude-projects` KV bucket uses key format `{clusterId}.{projectId}` (controller writes project status with cluster-prefixed keys, and KV JWT scope is `$KV.mclaude-projects.{clusterId}.>`). The `spec-state-schema.md` KV section lists key format as `{userId}.{projectId}`. ADR-0024's KV table says the new key is `{uslug}.{pslug}` — but this conflicts with ADR-0011's `{clusterId}.{projectId}` format. The ADR-0024 provides no reconciliation of which format is authoritative for `mclaude-projects` after both ADRs apply, and does not address how cluster-scoped controller JWT grants (`$KV.mclaude-projects.{clusterId}.>`) change under the new scheme.
   - **Doc**: "`mclaude-projects` | `{userId}.{projectId}` | `{uslug}.{pslug}`".
   - **ADR-0016**: Controller JWT scope is `$KV.mclaude-projects.{clusterId}.>`. ADR-0011 shows keys as `{clusterId}.{projectId}`. These are contradictory with the ADR-0024 mapping.
