## Run: 2026-04-26T00:00:00Z

**Gaps found: 4**

---

1. **`mclaude-session-agent` compile gap is NOT closed — `subj` call sites still use non-existent 2-arg helpers** — The ADR claims "session-agent compiles after stage 3," but the actual call sites in `agent.go` call `subj.UserProjectAPISessionsCreate`, `subj.UserProjectAPITerminal`, and `subj.UserProjectLifecycle` — none of which exist in `mclaude-common/pkg/subj/subj.go`. The package exports only the 3-arg `UserHostProject*` variants (e.g. `UserHostProjectAPISessionsCreate(u, h, p)`). Similarly `state.go` lines 70 and 76 call `subj.SessionsKVKey(userSlug, projectSlug, sessionSlug)` (3 args) and `subj.ProjectsKVKey(userSlug, projectSlug)` (2 args), but the actual signatures are `SessionsKVKey(u, h, p, s)` (4 args) and `ProjectsKVKey(u, h, p)` (3 args). `daemon_jobs.go` lines 377 and 491 also call `SessionsKVKey` with 3 args. These are compile errors; the binary cannot be built in its current state. The ADR enumerates these as fix sites but the fixes have not been applied.
   - **Doc**: "After stage 3 (mclaude-session-agent): session-agent compiles." and "7 enumerated call sites — replace `subj.UserProject…` with `subj.UserHostProject…`"
   - **Code**: `mclaude-session-agent/agent.go:285,547,1136,1149,1163,1179` call `subj.UserProjectAPISessionsCreate`, `subj.UserProjectAPITerminal`, `subj.UserProjectLifecycle` (2-arg forms that do not exist). `state.go:70,76` call `SessionsKVKey` with 3 args and `ProjectsKVKey` with 2 args against 4-arg and 3-arg definitions respectively. `daemon_jobs.go:377,491` call `SessionsKVKey` with 3 args.

2. **`daemon.go` fixes are NOT applied — `mclaude-laptops`, `runLaptopHeartbeat`, `laptopsKV`, and `UserAPIProjectsCreate` all survive** — The ADR lists six edits required in `daemon.go`: add `HostSlug` to `DaemonConfig`, remove `UserAPIProjectsCreate` subscription, remove `LaptopsKVKey` invocations, remove the `laptopsKV` field, remove the `mclaude-laptops` bucket open call, and remove `runLaptopHeartbeat`. None of these edits are present. `daemon.go` still opens `mclaude-laptops` (line 85), still holds `laptopsKV` on the struct (line 57), still calls `subj.LaptopsKVKey` (lines 148, 170), still subscribes to `subj.UserAPIProjectsCreate` (line 126), and still spawns `runLaptopHeartbeat` (line 134). `DaemonConfig` has no `HostSlug` field. These are compile errors (the removed bucket does not exist in the ADR world) and behavioral violations.
   - **Doc**: "Remove `laptopsKV` field on the daemon struct (line 57) and any `mclaude-laptops` bucket open call (line 85). Remove the `runLaptopHeartbeat` goroutine entirely. Remove `UserAPIProjectsCreate` subscription (currently at line 126). Remove `LaptopsKVKey` invocations (currently at lines 148 and 170). Add `HostSlug` to `DaemonConfig`."
   - **Code**: `mclaude-session-agent/daemon.go:57,85,107,126,134,148,170` — all the targeted removals are still present. `DaemonConfig` struct (lines 37–48) has no `HostSlug` field.

3. **`hosts` table DDL in the ADR omits `direct_nats_url` column** — The ADR's `CREATE TABLE hosts` schema (Data Model section) lists `js_domain`, `leaf_url`, `account_jwt`, `public_key`, and `user_jwt` but does not include `direct_nats_url`. This column is, however, present in `spec-state-schema.md` (the canonical authority), in the Decisions table ("cluster-shared fields (`js_domain`, `leaf_url`, `account_jwt`, `direct_nats_url`, and `public_key`)"), in User Flow B step 5, in the CLI Component Changes section, and in the CHECK constraint description. A developer implementing stage 1 (Postgres DDL) from the ADR's own schema block would produce a table missing `direct_nats_url`, which would then break subsequent stages that write the column.
   - **Doc**: ADR Data Model section CREATE TABLE hosts — `direct_nats_url` is absent. But ADR Decisions row "Host identity" lists it: "`direct_nats_url`... cluster-shared fields."
   - **Code**: `docs/spec-state-schema.md:64` — `direct_nats_url` is present as a column in the canonical `hosts` schema.

4. **`mclaude cluster register` CLI flag inconsistency — `--slug` vs `--name`** — User Flow B step 1 and `spec-helm.md` use `--slug us-east` as the flag name (`mclaude cluster register --slug us-east ...`), but the CLI Component Changes section says the subcommand signature is `mclaude cluster register --name … --jetstream-domain … --leaf-url …` (using `--name` with no `--slug`). A developer implementing the `cluster` subcommand cannot resolve which flag to implement, and the CLI flag name determines the Helm docs example.
   - **Doc**: User Flow B step 1: `mclaude cluster register --slug us-east --jetstream-domain us-east --leaf-url ... --direct-nats-url ...`. CLI Component Changes: `mclaude cluster register --name … --jetstream-domain … --leaf-url …`. spec-helm.md line 128: `mclaude cluster register --slug us-east --jetstream-domain us-east ...`
   - **Code**: No CLI implementation exists yet; the inconsistency lives entirely in the spec/ADR text.
