## Run: 2026-04-15T00:00:00Z

**Gaps found: 12**

1. **Admin mux dispatch is a single catch-all handler — new routes are unroutable as written** — The doc says `POST /admin/clusters`, `GET /admin/clusters`, `POST /admin/clusters/{id}/members`, and `DELETE /admin/clusters/{id}/members/{userId}` are added to the admin mux. The existing `AdminMux()` in `mclaude-control-plane/server.go:39` registers exactly one handler: `mux.Handle("/admin/", s.adminAuthMiddleware(http.HandlerFunc(s.handleAdminUsers)))`. All `/admin/` traffic goes to `handleAdminUsers`. The doc never specifies whether the new cluster endpoints should extend `handleAdminUsers` with new cases, be registered as separate `mux.HandleFunc` patterns, or result in a new dispatch function. A developer cannot determine which approach to use without making a choice not specified in the document.
   - **Doc**: "New HTTP endpoints — all cluster management endpoints are on the admin mux (:9091)"
   - **Code**: `server.go:39` — single `/admin/` catch-all

2. **NATS leaf node auth model is incompatible with the current NATS server config** — The doc states leaf nodes authenticate with a "leaf NKey pair" and that user JWTs are valid on hub and all workers because all NATS instances share the account public key. But the current NATS server config (`charts/mclaude/templates/nats-configmap.yaml`) has no `authorization {}`, `accounts {}`, or operator JWT configuration — it runs with no server-side auth enforcement. NATS JWTs only work if the server is configured with the matching account public key in an `authorization { users: [] }` or full operator/account model. The design doesn't specify what NATS server config changes are required (account block, operator, resolver), leaving the hub and worker NATS configuration incomplete.
   - **Doc**: "One account NKey across hub and all workers. CP signs user JWTs once — valid everywhere."
   - **Code**: `nats-configmap.yaml` — no authorization/accounts block present

3. **`Migrate()` is a single idempotent DDL string — multi-step migration with backfill cannot run as described** — The doc specifies a "multi-step migration": (1) create `clusters` table, (2) `ALTER TABLE projects ADD COLUMN IF NOT EXISTS cluster_id` as nullable, (3) backfill NULL rows with the auto-registered cluster's ID, (4) `ALTER TABLE projects ALTER COLUMN cluster_id SET NOT NULL`. The existing `Migrate()` in `mclaude-control-plane/db.go:53` executes a single `const schema` string in one `pool.Exec()` call. Step 3 (backfill) requires the auto-registration logic to have already run and produced a cluster ID — it cannot be embedded in a DDL-only string. The doc never specifies how the backfill step is sequenced with respect to auto-registration, what happens to `Migrate()` itself (does it gain new schema only, or does it drive backfill too?), or where the backfill code lives.
   - **Doc**: "Add a multi-step migration to the schema string: (1)…(3) for existing rows with NULL cluster_id, the single-cluster auto-registration…backfills them"
   - **Code**: `db.go:53-56` — `Migrate()` runs a single DDL string, no procedural logic

4. **`projects.go` `CreateProject` signature must change but the doc doesn't specify how** — The current `CreateProject` in `db.go:119` takes `(ctx, id, userID, name, gitURL)` and inserts without `cluster_id`. The NATS subscriber in `projects.go:68` calls `db.CreateProject(...)` with no cluster parameter. The doc says `POST /api/projects` now accepts optional `clusterId` and the Postgres row must include it, but the project creation path today is a NATS subscriber (`mclaude.*.api.projects.create`), not an HTTP endpoint. The doc never specifies: (a) whether the NATS project-create message gains a `clusterId` field, (b) whether `CreateProject` DB method is updated to accept `clusterID`, or (c) whether the project-create subject/handler is replaced by an HTTP endpoint. A developer cannot determine the full call path for project creation.
   - **Doc**: "`POST /api/projects` now accepts optional `clusterId`"
   - **Code**: `projects.go:44` — subscription on `mclaude.*.api.projects.create`; `db.go:119` — `CreateProject` has no cluster_id parameter

5. **Worker controller system NKey: how it is used to authenticate to worker NATS is not specified** — The doc says the control plane generates "a system NKey pair" for the worker controller during cluster registration, distinct from leaf NKeys and user NKeys. The worker controller "connects to the local worker NATS using a system-level NKey credential." But NATS NKey-based auth requires either an NKey-only auth block (server trusts a list of public keys) or full JWT/account model. The doc never specifies: (a) how the worker NATS is configured to accept this system NKey, (b) what subject permissions the system NKey gets, (c) whether it is a standalone NKey or wrapped in a JWT, and (d) where in the worker NATS config the trust for this key is declared. A developer cannot configure the worker NATS or the controller connection.
   - **Doc**: "The worker NATS is configured to trust this system NKey for subscribing to `mclaude.clusters.>` subjects."
   - **Code**: No worker NATS exists yet; the current NATS config has no authorization block.

6. **`INATSClient.kvWatch`, `kvGet`, and `jsSubscribe` domain parameter: the existing interface and implementation don't support it** — The doc says these three methods gain an optional `domain` parameter. The current `INATSClient` interface in `types.ts:29-33` has fixed signatures: `jsSubscribe(stream, subject, startSeq, callback)`, `kvWatch(bucket, key, callback)`, `kvGet(bucket, key)`. The real `NATSClient` in `nats-client.ts` implements them without domain. The doc doesn't specify the exact TypeScript interface changes (new optional param position, overload vs union type), nor does it specify how `nats.ws` is called with domain routing — the `nats.ws` library has a specific API for domain-qualified JetStream that must be documented. A developer cannot update the interface or implementation without researching the nats.ws API separately.
   - **Doc**: "`INATSClient` gains an optional `domain` parameter on JetStream methods"
   - **Code**: `types.ts:24-38` — current `INATSClient` interface; `nats-client.ts:81-115`, `138-176`, `178-190` — current implementations

7. **SPA direct-to-worker connection lifecycle is unspecified: who owns it and when it is torn down** — The doc says "The SPA maintains up to two NATS connections." It says the direct worker connection is "opened on demand" when a session is opened. But it never specifies: (a) which object owns the second `NATSClient` instance, (b) whether the second connection is per-session or per-cluster, (c) whether it is closed when the user navigates away from the session, and (d) what happens if the user opens sessions on two different worker clusters simultaneously. The `AuthStore`, `SessionStore`, and `EventStore` each hold a reference to one `INATSClient` — the doc doesn't say how the second connection is threaded through these.
   - **Doc**: "SPA maintains up to two NATS connections… direct worker connection (opened on demand)"
   - **Code**: `auth-store.ts`, `session-store.ts`, `event-store.ts` — all accept a single `INATSClient`

8. **Login response type change is not propagated: `AuthTokens` and `AuthClient` are unspecified** — The doc adds `projects` and `clusters` arrays to the login response. The current `AuthTokens` interface in `types.ts:41-45` has only `jwt`, `nkeySeed`, `userId`, `natsUrl`. `AuthClient.login()` returns `AuthTokens`. The doc never specifies: (a) whether `AuthTokens` is extended with `projects` and `clusters` fields, (b) where in the SPA the cluster/project list from login is stored, or (c) which component opens the per-cluster KV watches on login. Without knowing which store holds the cluster list, a developer cannot wire the domain-qualified KV watches shown in the doc's pseudocode.
   - **Doc**: "Modified login response — add cluster info" with `projects` and `clusters` arrays
   - **Code**: `types.ts:41-45` — `AuthTokens` has 4 fields; `auth-client.ts:9-19` — returns `AuthTokens` directly

9. **`is_default` uniqueness constraint mechanism is not specified** — The doc says `user_clusters` has the constraint "at most one `is_default = TRUE` per `user_id`". But it never specifies how this is enforced: a partial unique index (`WHERE is_default = TRUE`), a CHECK constraint, a trigger, or pure application logic. The existing `Migrate()` uses plain DDL strings, and Postgres partial unique index syntax differs from a table constraint. A developer must choose an enforcement mechanism without guidance.
   - **Doc**: "Constraint: at most one `is_default = TRUE` per `user_id`"
   - **Code**: `db.go:151-168` — existing schema uses no partial indexes or triggers; mechanism unspecified

10. **Single-cluster backwards compatibility: "same NATS instance as hub and worker" is contradictory with separate JetStream domains** — The doc states a single-cluster deployment can use "the same NATS instance (worker configured as leaf of localhost, or a combined chart)." But a NATS instance cannot be both a hub and a leaf of itself — a leaf node connects to a remote hub; you cannot form a leaf-of-self topology. The doc leaves the actual single-cluster NATS config undefined, which is the primary deployment scenario for existing users. It mentions "a combined chart that deploys one NATS with both hub and worker JetStream domains" but never specifies how one NATS server exposes two JetStream domains simultaneously (NATS does not natively support multiple JetStream domains on a single server instance).
    - **Doc**: "A single-cluster deployment installs both charts on the same cluster. The hub NATS and worker NATS can be the same NATS instance (worker configured as leaf of localhost, or a combined chart...)"
    - **Code**: `nats-configmap.yaml` — current single NATS server; no multi-domain or leaf-self topology possible

11. **Provisioning NATS request timeout value and project `pending` status retry mechanism are unspecified** — The doc says when the provisioning NATS request times out, the control plane returns 503 and "Project record created in Postgres but status set to `pending`. Provisioning retried when cluster reconnects." But: (a) the timeout duration for the NATS request/reply is not given, (b) there is no `status` field in the current `projects` table (`db.go:162`) beyond the existing `status` column — it's unclear if `pending` is a new state or reuses the existing `active` default, (c) "provisioning retried when cluster reconnects" is stated as behavior but no mechanism (subscription, polling loop, reconnect hook) is described.
    - **Doc**: "Control plane returns 503 with error message. Project record created in Postgres but status set to `pending`. Provisioning retried when cluster reconnects."
    - **Code**: `db.go:162` — `status TEXT NOT NULL DEFAULT 'active'`; no `pending` status or retry logic exists

12. **`DELETE /admin/clusters/{id}/members/{userId}` conflict check references "active projects" but no detection mechanism is specified** — The doc says this endpoint returns 409 if the user "has active projects on this cluster." But it never specifies: (a) what SQL query detects this (join `projects` on `cluster_id = id AND user_id = userId AND status != 'stopped'`?), (b) what "active" means for a project in terms of the `status` column values, or (c) whether the check is advisory or transactional (race between project creation and grant revocation). The existing `projects` table only has a generic `status` column with no enumerated values specified in the current codebase.
    - **Doc**: "409 if user has active projects on this cluster"
    - **Code**: `db.go:162` — `status` column exists but no enumerated values are defined; no FK or query path specified

## Run: 2026-04-15T12:00:00Z

**Gaps found: 14**

1. **`INATSClient` domain parameter additions are not in the interface** — The design extends `INATSClient.kvWatch`, `kvGet`, and `jsSubscribe` with an optional `domain` parameter, but the existing interface in `mclaude-web/src/types.ts` (lines 29-33) has none. The design says "INATSClient gains an optional `domain` parameter as the last argument on JetStream methods" but does not specify the updated TypeScript interface signatures. The existing `NATSClient` implementation (`transport/nats-client.ts`) calls `nc.jetstream()` with no domain argument throughout. A developer must know the exact new signatures and whether `MockNATSClient` (used by all tests) must also be updated.
   - **Doc**: "INATSClient gains an optional `domain` parameter as the last argument on JetStream methods"
   - **Code**: `INATSClient` interface at `mclaude-web/src/types.ts:29-33` — no domain param. `NATSClient.jsSubscribe` at `transport/nats-client.ts:81` takes `_stream, subject, startSeq, callback` with no domain.

2. **`AuthTokens` type extension is incompatible with how `AuthClient` deserializes the login response** — The design adds `projects` and `clusters` arrays to `AuthTokens`, and says "`AuthStore` stores these after login". But `AuthClient.login()` at `transport/auth-client.ts:15` does `const tokens: AuthTokens = await res.json()` — it will populate whatever fields exist in `AuthTokens`. The existing `AuthTokens` type (`types.ts:40-45`) has `jwt, nkeySeed, userId, natsUrl?`. A developer must know: (a) the exact new field names and types to add to `AuthTokens`, (b) whether `AuthClient` needs changes, (c) what shape `projects` and `clusters` arrays take in the `AuthTokens` interface (distinct from the login response JSON). The design gives the JSON shape but not the TypeScript type definition.
   - **Doc**: "The `AuthTokens` type gains `projects` and `clusters` arrays matching the JSON above."
   - **Code**: `AuthTokens` at `mclaude-web/src/types.ts:40-45` — no `projects` or `clusters` field. `AuthClient.login()` at `transport/auth-client.ts:15` casts the response directly to `AuthTokens`.

3. **`SessionStore` constructor and initialization path for multi-cluster KV watches is unspecified** — The design says "`SessionStore` reads `clusters` from `AuthStore` on initialization to open per-cluster KV watches." But `SessionStore` currently takes `(natsClient: INATSClient, userId: string)` and calls `startWatching()` with no cluster awareness. A developer must decide: does the constructor signature change? Does `startWatching()` gain a `clusters` parameter? Or is there a new method? Who calls `startWatching()` and when? The design gives a code snippet showing the loop but does not specify where it lives or whether it replaces or supplements the existing `startWatching()` call.
   - **Doc**: "`SessionStore` reads `clusters` from `AuthStore` on initialization to open per-cluster KV watches."
   - **Code**: `SessionStore` at `mclaude-web/src/stores/session-store.ts:8-18` — constructor takes `(natsClient: INATSClient, userId: string)` only. `startWatching()` calls `natsClient.kvWatch` without domain.

4. **`SessionKVState` is not extended with cluster metadata** — The design says "Each `SessionKVState` is tagged with cluster metadata: `clusterId`, `clusterName`, and `jsDomain` are added to the KV update callback, not stored in the KV value itself." This implies the callback signature for `kvWatch` in the session-list context changes, or `SessionKVState` gains optional fields, or a new wrapper type is introduced. None of these are specified. The existing `SessionKVState` in `types.ts:90-104` has no cluster fields. The `SessionStore` currently stores sessions in `Map<string, SessionKVState>`. A developer must know what type the aggregated session list uses and how cluster metadata is threaded through.
   - **Doc**: "`clusterId`, `clusterName`, and `jsDomain` are added to the KV update callback, not stored in the KV value itself"
   - **Code**: `SessionKVState` at `mclaude-web/src/types.ts:90-104` — no cluster fields. `SessionStore._sessions` typed as `Map<string, SessionKVState>`.

5. **`EventStore` connection strategy is unspecified at the code level** — The design says "`EventStore` reads `projects` to look up `jsDomain` and `directNatsUrl` for the active session's project" and owns the direct worker connection. But `EventStore` currently takes `EventStoreOptions` with `natsClient, userId, projectId, sessionId` (`event-store.ts:21-26`). The design does not specify: whether `EventStoreOptions` gains new fields, whether `EventStore.start()` gains a parameter, how the direct connection is passed in or created internally, or how fallback is signaled to the rest of the UI. A developer cannot implement this without stopping to ask about the new constructor/option shape.
   - **Doc**: "`EventStore` reads `projects` to look up `jsDomain` and `directNatsUrl` for the active session's project."
   - **Code**: `EventStoreOptions` at `mclaude-web/src/stores/event-store.ts:21-26` — fields: `natsClient, userId, projectId, sessionId`. No domain or directNatsUrl.

6. **`POST /admin/clusters` response includes `hubLeafPort` and `accountPubKey` but neither is defined anywhere** — The registration response schema lists `{ id, name, jsDomain, leafCreds, hubNatsUrl, hubLeafPort, accountPubKey, status }`. The `hubLeafPort` is implied by the NATS config (`leafnodes { port: 7422 }`) but the design does not state how the control plane knows its own external leaf port, nor what env var or config supplies it. The `accountPubKey` must be derived from the account NKey, but there is no specification of how the control plane exposes this value at registration time.
   - **Doc**: "Response (201): `{ id, name, jsDomain, leafCreds, hubNatsUrl, hubLeafPort, accountPubKey, status }`"
   - **Code**: `Server` struct at `mclaude-control-plane/auth.go:38-48` — `natsWsURL` and `natsURL` fields exist, but no `hubLeafPort` or leaf-listener URL. No env var for leaf port is defined anywhere in `main.go` or `values.yaml`.

7. **Worker controller NKey seed is returned by `POST /admin/clusters` but is absent from the documented response schema** — Section "Worker Controller" says "The control plane generates a controller NKey pair during cluster registration (separate from user NKeys and leaf NKeys). The NKey seed is included in the `POST /admin/clusters` registration response." But the registration response schema in Component Changes lists `{ id, name, jsDomain, leafCreds, hubNatsUrl, hubLeafPort, accountPubKey, status }` — `controllerNKeySeed` is absent from the schema. A developer implementing the endpoint does not know the field name for this credential in the response.
   - **Doc**: Response schema at lines 92-93 vs. "The NKey seed is included in the `POST /admin/clusters` registration response" at line 184.
   - **Code**: No existing implementation.

8. **Migration step 4 (`ALTER TABLE projects ALTER COLUMN cluster_id SET NOT NULL`) is not idempotent and has no failure specification** — The design says steps 1-4 run on control-plane startup. Step 4 will fail on subsequent startups if the column is already NOT NULL (depending on Postgres version behavior). If the backfill in step 3 cannot resolve all NULLs (e.g., auto-registration fails), step 4 will fail and block startup. The design does not specify: what happens if step 4 fails, whether the migration is idempotent across restarts, or how startup failure is surfaced to operators.
   - **Doc**: "Steps 2-4 are procedural Go code in the control plane startup path" (line 85).
   - **Code**: `db.Migrate()` at `mclaude-control-plane/db.go:53-56` uses `CREATE TABLE IF NOT EXISTS` and `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` — idempotent. The NOT NULL ALTER is not idempotent with this pattern.

9. **Auto-registration user grant step is not specified to be idempotent across restarts** — The design says on auto-registration "grants all existing users access to this cluster with `is_default = TRUE`." But the `user_clusters` table has a partial unique index: at most one `is_default = TRUE` per user. If a user already has a `user_clusters` row from a prior startup, the re-run `INSERT INTO user_clusters ... is_default=TRUE` will fail with a unique constraint violation. The design does not specify whether to use `INSERT ... ON CONFLICT DO NOTHING` or upsert, nor the behavior on repeated startups.
   - **Doc**: "grants all existing users access to this cluster with `is_default = TRUE`" (line 332).
   - **Code**: `user_clusters` partial unique index defined at line 74: `CREATE UNIQUE INDEX idx_user_clusters_default ON user_clusters (user_id) WHERE is_default = TRUE`.

10. **`DELETE /admin/clusters/{id}/members/{userId}` 409 condition introduces `pending` project status that does not exist** — The error condition states "409 if user has projects on this cluster with `status IN ('active', 'pending')`." The design introduces `pending` as a new project status for provisioning-failed projects. However, the existing `projects` table schema at `db.go:162` has `status TEXT NOT NULL DEFAULT 'active'`. Neither the migration that adds `pending` as a recognized value nor any enforcement of the status enum is specified.
    - **Doc**: "409 if user has projects on this cluster with `status IN ('active', 'pending')`" (line 111). "Project record created in Postgres with `status = 'pending'`" (line 405).
    - **Code**: `projects` schema at `db.go:160-168` — `status TEXT NOT NULL DEFAULT 'active'`. `CreateProject` at `db.go:122` hardcodes `'active'`. No `pending` status exists today.

11. **Single-cluster mode: SPA behavior when `directNatsUrl` is empty is contradictory** — The design says "the SPA connects to this NATS directly without domain qualification." But the SPA's cluster-watch loop iterates `loginResponse.clusters` and calls `kvWatch(..., cluster.jsDomain)` for each cluster. If `jsDomain` is `"default"` (the auto-registered value), the SPA will pass a domain string to all KV watches anyway. The design does not specify how to detect that domain qualification should be skipped, whether an empty `jsDomain` means "no domain routing," or how the `jsDomain = "default"` auto-registered cluster value interacts with the hub's configured `domain: hub` (two different values in the same doc).
    - **Doc**: "SPA connects to this NATS directly without domain qualification" (line 322) vs. the session-list loop pseudocode at lines 229-237 which passes `cluster.jsDomain` unconditionally. Auto-registration uses `NATS_JS_DOMAIN` default `"default"` (line 327) but hub NATS data model specifies `domain: hub` (line 355).
    - **Code**: `NATSClient.kvWatch` at `transport/nats-client.ts:138` — no domain support currently.

12. **Hub NATS JetStream `domain: hub` conflicts with auto-registration `js_domain` default `"default"`** — The data model section specifies the hub NATS config has `domain: hub`. The auto-registration section says `js_domain` is the value of `NATS_JS_DOMAIN` env var defaulting to `"default"`. If a new single-cluster deployment auto-registers with `js_domain = "default"` but the hub NATS is configured with `domain: hub`, the SPA's domain-qualified KV watches for the hub cluster will use `"default"` while the actual JetStream domain is `"hub"` — resulting in failed KV watch connections. No resolution is stated.
    - **Doc**: Hub NATS config at line 355: `domain: hub`. Auto-registration at line 327: default `"default"`.
    - **Code**: `main.go` — `NATS_JS_DOMAIN` env var does not exist today; no NATS domain configured in `nats-configmap.yaml`.

13. **Worker controller NATS auth config snippet uses an invalid NATS configuration block** — The design shows `authorization { account: $MCLAUDE_ACCOUNT_PUBLIC_KEY users: [...] }`. In standard NATS server configuration, the `authorization` block accepts `users`, `token`, or `timeout` — `account` is not a recognized key inside `authorization`. Account-based JWT validation is configured at the server level in an `accounts` or `operator` block, not inside `authorization`. This snippet will either be silently ignored or cause a NATS parse error, making the controller authentication design non-functional as written.
    - **Doc**: Lines 187-193: `authorization { account: $MCLAUDE_ACCOUNT_PUBLIC_KEY users: [...] }`
    - **Code**: Existing NATS config at `charts/mclaude/templates/nats-configmap.yaml` — no `authorization` block; uses no auth today.

14. **Provisioning failure recovery via "pending" project re-send on heartbeat resume has no idempotency guarantee for the worker controller** — The design says when a cluster heartbeat resumes, the control plane re-sends provisioning NATS requests for all `status = 'pending'` projects. The worker controller receives these and creates MCProject CRDs. If the worker already created the CRD (provisioned successfully but the NATS reply timed out), the re-sent request will attempt to create a CRD that already exists. The design requires the worker controller `CreateMCProject` call to be idempotent, but this is not stated, and the NATS-based provisioning path for the worker does not inherit idempotency from the reconciler architecture (which handles drift, not duplicate creates).
    - **Doc**: "When the cluster heartbeat resumes...the control plane queries for projects with `status = 'pending'` on that cluster and re-sends provisioning requests" (line 405).
    - **Code**: No worker controller implementation exists yet; `projects.go:79-88` uses `CreateMCProject` which relies on the reconciler for idempotency but the NATS path does not.
