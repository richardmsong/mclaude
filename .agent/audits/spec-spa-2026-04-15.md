## Run: 2026-04-15T00:00:00Z

### Phase 1 — Spec → Code

| Spec (doc:line) | Spec text | Code location | Verdict | Notes |
|-----------------|-----------|---------------|---------|-------|
| plan-replay-user-messages.md:116-128 | `UserEvent` interface adds `uuid?`, `isReplay?`, `isSynthetic?` fields | types.ts:181-190 | IMPLEMENTED | All three optional fields present |
| plan-replay-user-messages.md:130-148 | Add `SystemMessageBlock` with type `'system_message'` and include it in `Block` union | types.ts:282-300 | IMPLEMENTED | `SystemMessageBlock` interface and union present |
| plan-replay-user-messages.md:152-158 | `PendingMessage` interface with `uuid`, `content`, `sentAt` | types.ts:302-306 | IMPLEMENTED | Exactly as spec'd |
| plan-replay-user-messages.md:162 | `_pendingMessages: PendingMessage[]` new state in EventStore | event-store.ts:40 | IMPLEMENTED | |
| plan-replay-user-messages.md:164 | `addPendingMessage(uuid, content)` — adds to `_pendingMessages`, notifies listeners, does NOT add to `_conversation.turns` | event-store.ts:171-175 | IMPLEMENTED | |
| plan-replay-user-messages.md:166-174 | Modified `case 'user'` handler — step 1: if tool_result, attach to ToolUseBlock and return early | event-store.ts:358-387 | IMPLEMENTED | |
| plan-replay-user-messages.md:169 | Step 2: if event has `uuid` and matching pending exists, remove from `_pendingMessages` | event-store.ts:389-395 | IMPLEMENTED | |
| plan-replay-user-messages.md:170 | Step 3: if `isSynthetic`, create system turn with `SystemMessageBlock` | event-store.ts:397-413 | IMPLEMENTED | |
| plan-replay-user-messages.md:171 | Step 4: otherwise create normal user turn inline | event-store.ts:416-439 | IMPLEMENTED | |
| plan-replay-user-messages.md:172 | Remove text-matching dedup logic | event-store.ts:357-439 | IMPLEMENTED | No text-matching dedup present |
| plan-replay-user-messages.md:174 | `clear` and `compact_boundary` handlers must also clear `_pendingMessages = []` | event-store.ts:217-218, 243-244 | IMPLEMENTED | Both reset `_pendingMessages` |
| plan-replay-user-messages.md:176 | `get pendingMessages()` getter exposes pending list | event-store.ts:177-179 | IMPLEMENTED | |
| plan-replay-user-messages.md:181-192 | `ConversationVMState` adds `pendingMessages: PendingMessage[]` field | conversation-vm.ts:6-13 | IMPLEMENTED | |
| plan-replay-user-messages.md:193-194 | `ConversationVM.state` getter reads `this.eventStore.pendingMessages` | conversation-vm.ts:33-46 | IMPLEMENTED | |
| plan-replay-user-messages.md:195-199 | `sendMessage(text)` — generate uuid, call `addPendingMessage`, publish with uuid | conversation-vm.ts:49-62 | IMPLEMENTED | |
| plan-replay-user-messages.md:199 | No `sending` guard — function is synchronous, send button stays enabled | conversation-vm.ts:49-62, SessionDetailScreen.tsx:947-965 | IMPLEMENTED | No `sending` state in send flow |
| plan-replay-user-messages.md:201-202 | `sendMessageWithImage(text, imageBase64, mimeType)` — same pattern with uuid | conversation-vm.ts:64-83 | IMPLEMENTED | |
| plan-replay-user-messages.md:205-208 | Render `pendingMessages` at bottom of message list with opacity 0.5 and "sending..." indicator | EventList.tsx:150-171 | IMPLEMENTED | opacity 0.5, "sending..." text |
| plan-replay-user-messages.md:208 | Send button stays enabled at all times except when NATS disconnected | SessionDetailScreen.tsx:947-965 | IMPLEMENTED | Disabled only when `!input.trim() || !connected` |
| plan-replay-user-messages.md:212 | `EventList` renders `SystemMessageBlock` (type `system_message`) as gray, smaller text — same style as CompactionBlock | EventList.tsx:89-102 | PARTIAL | Rendered as small gray italic text, but CompactionBlock uses `SystemEvent` component (centered, --text3), while `system_message` uses a plain `<div>` inline; spec says "same style as CompactionBlock" |
| plan-replay-user-messages.md:231-233 | uuid optional field in `api.sessions.input` NATS payload | conversation-vm.ts:53-59 | IMPLEMENTED | `uuid` field included |
| plan-replay-user-messages.md:233 | `uuid` optional for backward compat; if absent, pending matching falls back to text comparison | event-store.ts:389-395 | GAP | Spec says "If absent, pending matching falls back to text comparison." Code only checks `event.uuid` — no text-comparison fallback. Pending message stays stuck if uuid is missing from replay. |
| plan-client-architecture.md:39-55 | `NATSClient` with `connect`, `reconnect`, `subscribe`, `publish`, `request`, `kvWatch`, `kvGet`, `onDisconnect`, `onReconnect` | nats-client.ts, types.ts:24-38 | IMPLEMENTED | All methods present |
| plan-client-architecture.md:59-74 | `AuthClient` with `login`, `loginSSO`, `refresh`, `logout`, `loadFromStorage`, persists JWT/nkeySeed/userId/natsUrl | auth-client.ts | IMPLEMENTED | |
| plan-client-architecture.md:83-101 | `AuthStore` with `login`, `loginSSO`, `logout`, `restoreTokens`, `startRefreshLoop`, JWT refresh when TTL below threshold | auth-store.ts | IMPLEMENTED | |
| plan-client-architecture.md:104-149 | `SessionStore` with `sessions`, `projects`, `startWatching`, `getSessionsForProject`, `onSessionChanged`, `onProjectChanged` | session-store.ts | IMPLEMENTED | |
| plan-client-architecture.md:107 | `SessionStore.heartbeats` field | session-store.ts | GAP | Spec shows `heartbeats: Map<projectId, { ts, healthy }>` as a field on SessionStore. Heartbeats are handled by HeartbeatMonitor (separate class), not as a field on SessionStore. Minor architecture variance. |
| plan-client-architecture.md:118-120 | `SessionStore.isAgentHealthy(projectId)` method | session-store.ts | GAP | Not implemented on SessionStore. Implemented in `HeartbeatMonitor.isHealthy()` instead. |
| plan-client-architecture.md:123 | `SessionStore.onHealthChanged(callback)` | session-store.ts | GAP | Not on SessionStore. On HeartbeatMonitor instead. |
| plan-client-architecture.md:156-172 | `EventStore` with `events: Event[]` field | event-store.ts | GAP | `events: Event[]` field is not present. The EventStore only maintains `_conversation` (the accumulated model) and `_pendingMessages`, not a raw event list. |
| plan-client-architecture.md:163-164 | `EventStore.onEvent(callback)` | event-store.ts | GAP | No `onEvent` callback — only `onConversationChanged`. |
| plan-client-architecture.md:168 | `EventStore` reads `replayFromSeq` from SessionStore KV before subscribing | event-store.ts:68-71, App.tsx:241-243 | IMPLEMENTED | App.tsx reads it from session KV and passes to `store.start()` |
| plan-client-architecture.md:246-256 | Deduplication: skip events whose JetStream seq <= `lastSequence` | event-store.ts:87-91 | IMPLEMENTED | |
| plan-client-architecture.md:248 | `user` (human text, not tool_result) → dedup: if last turn is user turn with matching text, skip | event-store.ts:357-439 | IMPLEMENTED | Text-matching dedup replaced by uuid matching per plan-replay-user-messages spec |
| plan-client-architecture.md:251 | On `clear` event: reset `ConversationModel`, update local `replayFromSeq` | event-store.ts:206-224 | IMPLEMENTED | |
| plan-client-architecture.md:252 | On `compact_boundary`: reset, add CompactionBlock, update `replayFromSeq` | event-store.ts:226-249 | IMPLEMENTED | |
| plan-client-architecture.md:253 | Tracks `lastSequence` for replay on reconnect | event-store.ts:48-50 | IMPLEMENTED | |
| plan-client-architecture.md:254 | On reconnect: re-subscribes from `max(lastSequence + 1, replayFromSeq)` | event-store.ts | GAP | EventStore does not implement reconnect logic — no re-subscribe on reconnect. The stop/start lifecycle is managed externally but no code path handles `max(lastSequence+1, replayFromSeq)` on NATS reconnect. |
| plan-client-architecture.md:270-296 | `LifecycleStore` subscribing to `mclaude.{userId}.{location}.{projectId}.lifecycle.>` | lifecycle-store.ts:17 | PARTIAL | Subscribes to `mclaude.${userId}.${projectId}.lifecycle.>` — missing `{location}` wildcard. Spec uses `mclaude.{userId}.{location}.{projectId}` pattern. |
| plan-client-architecture.md:280-295 | `HeartbeatMonitor` with `start(checkIntervalMs)`, `stop`, `isHealthy`, `onHealthChanged` | heartbeat-monitor.ts | IMPLEMENTED | |
| plan-client-architecture.md:295 | `HeartbeatMonitor` threshold default 60s | heartbeat-monitor.ts:21 | IMPLEMENTED | |
| plan-client-architecture.md:306-314 | `SessionListVM` with `projects: ProjectVM[]`, `createProject`, `deleteProject`, `createSession`, `deleteSession` | session-list-vm.ts | PARTIAL | `deleteProject` method is not implemented |
| plan-client-architecture.md:340-355 | `ConversationVM` with `sendMessage`, `sendMessageWithImage`, `approvePermission`, `denyPermission`, `interrupt`, `switchModel`, `invokeSkill`, `reloadPlugins` | conversation-vm.ts | IMPLEMENTED | All actions present |
| plan-client-architecture.md:371-375 | `ConversationVM.sendMessage` publishes `{"type": "user", "message": {...}}` to `.api.sessions.input` | conversation-vm.ts:52-62 | IMPLEMENTED | |
| plan-client-architecture.md:376-378 | `ConversationVM` tracks streaming state (is Claude currently typing?) | conversation-vm.ts:36-38 | IMPLEMENTED | `isStreaming` field in state |
| plan-client-architecture.md:403-423 | `PermissionPromptVM` with `pending`, `approve`, `deny` | permission-prompt-vm.ts | IMPLEMENTED | |
| plan-client-architecture.md:426-437 | `SkillsPickerVM` with `skills`, `invoke`, `refresh` | skills-picker-vm.ts | IMPLEMENTED | |
| plan-client-architecture.md:440-466 | `TerminalVM` with `createTerminal`, `deleteTerminal`, `sendInput`, `resize`, `onOutput` | terminal-vm.ts | IMPLEMENTED | |
| plan-client-architecture.md:463-483 | NATS subscribe subjects including wildcard `{location}` — `mclaude.{userId}.{location}.{projectId}.events.{sessionId}` | event-store.ts:69 | GAP | EventStore subscribes to `mclaude.${userId}.${projectId}.events.${sessionId}` — missing `{location}` wildcard. Spec requires wildcard for multi-cluster support. |
| plan-client-architecture.md:488-494 | NATS KV watches: `mclaude-sessions`, `mclaude-projects`, `mclaude-heartbeats` | session-store.ts, heartbeat-monitor.ts | IMPLEMENTED | |
| plan-client-architecture.md:497-507 | Client must handle event types: system.init, session_state_changed, stream_event, assistant, user, control_request, tool_progress, result | event-store.ts:204-490 | IMPLEMENTED | All event types handled |
| plan-client-architecture.md:515-554 | Reconnection strategy: visibilitychange reconnect on mobile Safari | App.tsx:123-138 | IMPLEMENTED | |
| plan-client-architecture.md:561-643 | `AuthClient.loadFromStorage()` — expose method so app can restore session | auth-client.ts:65-75 | IMPLEMENTED | |
| plan-client-architecture.md:683-695 | `ConversationModel` uses `replayFromSeq` from session KV for `start()` | App.tsx:240-243 | IMPLEMENTED | |
| ui-spec.md:11-29 | Dark theme color palette with specific CSS variable tokens | main.tsx or CSS | IMPLEMENTED | CSS custom properties used throughout via `var(--bg)`, `var(--surf)`, etc. |
| ui-spec.md:64-75 | Hash-based routing for `#`, `#s/{sessionId}`, `#settings`, `#usage`, `#users` | App.tsx:20-29 | IMPLEMENTED | |
| ui-spec.md:79-104 | Auth/Login screen: icon, title, password field, Connect button; Return/Enter submits; error shown inline | AuthScreen.tsx | PARTIAL | Login screen uses email+password, not just a single token field. Spec shows only a password/token field with subtitle "Enter your access token". The code adds an email field not in the spec wireframe. However, the rest (error shown inline, button, etc.) is correct. |
| ui-spec.md:108-125 | First-Run Flow: create project if none, create session "Getting Started", navigate, send onboarding message | App.tsx:170-198 | IMPLEMENTED | |
| ui-spec.md:130-151 | Dashboard nav bar: title "MClaude", badge, 📊 usage, ⚙ settings, ⋯ menu, connection dot | DashboardScreen.tsx, NavBar.tsx | IMPLEMENTED | |
| ui-spec.md:152 | Badge appears when sessions have `needs_permission` or `plan_mode` status | DashboardScreen.tsx:66-68 | PARTIAL | Badge shown for `requires_action` and `hasPendingPermission` but not for `plan_mode` state |
| ui-spec.md:154-165 | Dashboard overflow menu (⋯): "New Project" option | DashboardScreen.tsx:108-147 | IMPLEMENTED | |
| ui-spec.md:170-181 | Tmux filter chips shown when >1 tmux session name; pill shape; default "mclaude" chip | DashboardScreen.tsx:84-188 | PARTIAL | Filter chips present but they filter by project name (not tmux session name). Spec requires filtering by tmux session names. The code also uses "all" not "All" chip label plus chips are alphabetical by project name, not tmux session name. Also spec says default chip is "mclaude" if present — code defaults to "all". |
| ui-spec.md:183-196 | Session rows: status dot, project name, status label, cwd path, chevron, tap to navigate | DashboardScreen.tsx:280-307 | IMPLEMENTED | |
| ui-spec.md:198-211 | Dashboard empty state: project list when sessions=0 but projects>0; "Tap + to start a session" | DashboardScreen.tsx:218-276 | IMPLEMENTED | |
| ui-spec.md:213-235 | FAB: 52px circle, --blue, bottom-right 20px; 1 project = direct create; multi-project = sheet | DashboardScreen.tsx:311-332 | IMPLEMENTED | |
| ui-spec.md:247-263 | New Session sheet: bottom modal, project rows sorted by lastProjectId then alphabetically | NewSessionSheet.tsx | IMPLEMENTED | |
| ui-spec.md:267-299 | New Project sheet: Name required, Git URL optional, Create button, inline error | NewProjectSheet.tsx | IMPLEMENTED | |
| ui-spec.md:303-372 | Session Detail nav bar, det-meta row, plan card (plan_mode), action bar, tab bar, input bar | SessionDetailScreen.tsx | IMPLEMENTED | |
| ui-spec.md:320-322 | Three-dot menu: Model, Token Usage, Raw Output | SessionDetailScreen.tsx:388-499 | IMPLEMENTED | All three present |
| ui-spec.md:356-365 | Input bar: stop button (only working), attach, textarea, PTT, send | SessionDetailScreen.tsx:839-968 | IMPLEMENTED | |
| ui-spec.md:362-364 | When screenshot staged: preview strip with thumbnail + "Screenshot ready" + ✕ | SessionDetailScreen.tsx:776-799 | IMPLEMENTED | |
| ui-spec.md:366-374 | Skills autocomplete: popup above input bar when starts with `/`, tappable rows | SessionDetailScreen.tsx:800-837 | IMPLEMENTED | |
| ui-spec.md:378-388 | User messages: right-aligned bubble, --blue background, white text, border-radius 18px | UserMessage.tsx | IMPLEMENTED | |
| ui-spec.md:390-403 | Assistant text: full-width, markdown-rendered (headings, bold, italic, code blocks, lists, tables) | AssistantText.tsx | PARTIAL | Markdown renders headings, bold, italic, code blocks, unordered/ordered lists. Tables are NOT rendered (not in AssistantText.tsx). Spec says tables must be horizontally scrollable with --border separators. |
| ui-spec.md:406-413 | Thinking block: collapsible, collapsed="▶ Claude's thinking…" in --purple, expanded=monospace --surf2 | ThinkingBlock.tsx | IMPLEMENTED | |
| ui-spec.md:418-453 | Tool use+result: unified card, tool icons, tool-specific display, result section | ToolCard.tsx | IMPLEMENTED | All features including syntax highlighting, diff view, show-more |
| ui-spec.md:441 | Tool icon for `!` (Bash shorthand) should be ⚡ | ToolCard.tsx:6-16 | GAP | Icon for `!` is not in TOOL_ICONS. When `name === '!'`, `formatInput` handles it but `toolIcon('!')` returns `'🛠'` (default) not `'⚡'`. Spec says Bash/! → 💻/⚡. |
| ui-spec.md:457-481 | AskUserQuestion block: ❓ header, radio options, Submit/Cancel, "✓ Submitted" after | AskUserQuestion.tsx | IMPLEMENTED | |
| ui-spec.md:483-487 | Standalone tool_result (.ev-result): only shown when not paired with tool_use; monospace, left border | EventList.tsx:53-74 | IMPLEMENTED | |
| ui-spec.md:489-491 | System event (.ev-sys): centered, --text3 color, small text | SystemEvent.tsx | IMPLEMENTED | |
| ui-spec.md:493-507 | Subagent group (.ev-agent-group): collapsible, orange left border, event count badge | AgentGroup.tsx | IMPLEMENTED | |
| ui-spec.md:511-526 | Terminal tab: xterm.js canvas, keyboard toolbar | SessionDetailScreen.tsx:752-765 | PARTIAL | Terminal tab shows a placeholder div with "Terminal — connect via NATS". No xterm.js implementation present. Spec requires full xterm.js terminal. |
| ui-spec.md:529-549 | Event Detail Modal: bottom sheet, icon+timestamp+close, content fields | EventDetailModal.tsx | IMPLEMENTED | |
| ui-spec.md:552-568 | Three-dot menu dropdown: Model submenu, Token Usage, Raw Output | SessionDetailScreen.tsx:388-499 | IMPLEMENTED | |
| ui-spec.md:572-607 | Token Usage (session): 2x2 grid tiles, estimated cost | SessionDetailScreen.tsx:515-569 | IMPLEMENTED | |
| ui-spec.md:611-645 | Token Usage (global screen): time range chips, monthly budget bar, stacked bar chart | TokenUsage.tsx | PARTIAL | Time range chips implemented, token tiles, stacked bar, cost, calibration. BUT: monthly budget bar with projected overage is NOT implemented. Budget bar spec requires "solid for actual spend, semi-transparent for projected overage", thresholds at 60%/85%. Not in code. |
| ui-spec.md:648-697 | Settings screen: HOST, CONNECTION, ADMINISTRATION, ACCOUNT sections | Settings.tsx | PARTIAL | Has HOST (shows server URL), CONNECTION (status + session count), ACCOUNT (user ID). Missing: ADMINISTRATION section with User Management link (admin only), "Active Host" selector, "Connected Hosts" list. |
| ui-spec.md:700-714 | User Management screen: Invite User button, user list | UserManagement.tsx | IMPLEMENTED | Placeholder UI, no real data loading — but spec says this is an admin screen. |
| ui-spec.md:717-728 | Connection indicator dot: connected=green, connecting=gray pulsing, error=red, off=text3 | StatusDot.tsx | IMPLEMENTED | |
| ui-spec.md:730-750 | Inline diff view: GitHub-style unified diff, char-level highlights, gutter +/- | DiffView.tsx | IMPLEMENTED | Full char-level LCS diff, gutter, line colors |
| ui-spec.md:753-766 | Prompt bar: Claude `/ask` prompt with option buttons | — | GAP | No `Prompt bar` implementation found. Spec shows a separate "Prompt Bar" interaction pattern for Claude Code `/ask` prompts with numbered option buttons. Not implemented. |
| ui-spec.md:769-783 | Raw Output Overlay: ANSI-colored terminal text, live-refresh at 500ms | SessionDetailScreen.tsx:572-592 | PARTIAL | Raw output overlay implemented but shows extracted text from `vmState.turns`, not actual raw ANSI terminal output. Spec says ANSI escape codes rendered as colored text. No live-refresh at 500ms. |
| ui-spec.md:793-807 | Push-to-Talk: hold to record, release to send, red pulse while recording, fallback dimmed 40% | SessionDetailScreen.tsx:262-354 | IMPLEMENTED | |
| ui-spec.md:835-838 | Web SPA: React functional components, hash routing, nats.ws, xterm.js | App.tsx, nats-client.ts | PARTIAL | React + hash routing + nats.ws implemented. xterm.js is NOT used for terminal (placeholder only). |
| ui-spec.md:859-870 | v1 requirements: inline screenshot, subagent nesting, bash syntax highlighting, calibration, compaction, laptop/host filtering, live model switching, plan mode, scroll to bottom on send, tab memory | Various | PARTIAL | Screenshots: implemented. Subagent nesting: implemented. Bash syntax: implemented. Calibration: implemented. Compaction: implemented. Tab memory: implemented. Scroll to bottom: implemented. Plan mode card: stub (no plan fetch). Live model switching: implemented. Laptop/host filtering: NOT implemented (Settings shows server URL but no host switching). |
| plan-graceful-upgrades.md:338-352 | SPA: `SessionStore.onSessionAdded()` method | session-store.ts:119-128 | IMPLEMENTED | |
| plan-graceful-upgrades.md:355-408 | SPA: `createSession()` switches from request-reply to publish + KV watch with 30s timeout + api_error sub | session-list-vm.ts:72-116 | IMPLEMENTED | |
| plan-graceful-upgrades.md:410-420 | SPA: `deleteSession()` fire-and-forget | session-list-vm.ts:118-124 | IMPLEMENTED | |
| plan-graceful-upgrades.md:422-459 | SPA: kvWatch passes DEL entries with `operation` field; session-store checks `entry.operation` | nats-client.ts:156-163, session-store.ts:35-58 | IMPLEMENTED | |
| plan-graceful-upgrades.md:461-477 | SPA: Session Detail `updating` state → STATE_LABELS, updating banner above conversation | SessionDetailScreen.tsx:29-38, 715-736 | IMPLEMENTED | |
| plan-graceful-upgrades.md:493-502 | SPA: Dashboard `STATE_LABELS` add `updating`, type cast add `updating` | DashboardScreen.tsx:72-82, 295 | IMPLEMENTED | |
| plan-graceful-upgrades.md:504-519 | SPA: `StatusDot` add `updating` to `STATE_COLORS` (--blue) and `PULSE_STATES` | StatusDot.tsx:23, 28 | IMPLEMENTED | |
| plan-graceful-upgrades.md:521-536 | SPA: `types.ts` add `'updating'` to `SessionState` union and `'session_upgrading'` to `LifecycleEvent.type` | types.ts:79-88, 323-328 | IMPLEMENTED | |

### Phase 2 — Code → Spec

| File:lines | Classification | Explanation |
|------------|---------------|-------------|
| auth-client.ts:65-75 | UNSPEC'd | `loadFromStorage()` public method — spec shows `loadFromStorage()` under AuthClient responsibilities (plan-client-architecture.md:74). It is spec'd but named differently than `getStoredTokens()`. This method is used in App.tsx for token restoration. |
| auth-store.ts:70-89 | INFRA | `_checkAndRefresh()` — internal implementation of the spec'd "refresh when TTL < threshold" behavior. |
| auth-store.ts:91-99 | INFRA | `_decodeExpiry()` — JWT payload decoding needed for refresh logic. |
| conversation-vm.ts:148-152 | UNSPEC'd | `setMaxThinkingTokens(budget)` — publishes `set_max_thinking_tokens` control request. Not described in plan-client-architecture.md or ui-spec.md. Present in SessionDetailScreen's "Thinking Budget" menu. |
| session-list-vm.ts:62-69 | INFRA | `createProject()` — uses `request()` (old request-reply). plan-graceful-upgrades.md switches sessions.create/delete to publish+KV but createProject is not changed. The spec plan-client-architecture.md shows `createProject(name, gitUrl)` as request-reply, so this is correct. |
| event-store.ts:161-168 | INFRA | `applyEventForTest()` — test helper, excluded from production audit. |
| nats-client.ts:81-115 | INFRA | `jsSubscribe()` — implements the JetStream ordered consumer for EventStore. Required infra. |
| session-store.ts:6-7, 119-128 | INFRA | `SessionAddedListener` type and `_addListeners` array — supporting infrastructure for `onSessionAdded()`. |
| App.tsx:36-60 | UNSPEC'd | `UpdateBanner` component that polls for new SPA bundle versions and shows "New version available — tap to reload". Not described in any design doc. |
| App.tsx:256-257, hooks/useVersionPoller.ts | UNSPEC'd | Version polling hook — `useVersionPoller()` fetches index.html every 60s, detects bundle hash change. Not described in any design doc. |
| App.tsx:276-291 | UNSPEC'd | `handleCacheReset()` — clears session/heartbeat watchers and restarts them. Exposed as "Reset Client Cache" in Settings. Not described in any design doc. |
| SessionDetailScreen.tsx:435-463 | UNSPEC'd | "Thinking Budget" submenu in the three-dot menu with None/Low/Medium/High options (calls `setMaxThinkingTokens`). Not described in ui-spec.md three-dot menu spec. |
| DashboardScreen.tsx:70 | INFRA | `unhealthyProjects` filter and agent health banner (red banner "Agent down: ... heartbeat stale"). Not explicitly described in ui-spec.md Dashboard section. Infra for heartbeat health visibility. |
| permission-prompt-vm.ts:38-53 | UNSPEC'd | `allPending: PendingPermission[]` getter returns all pending (not just first). Plan-client-architecture only specifies `pending: PendingPermission | null`. |
| permission-prompt-vm.ts:77-90 | UNSPEC'd | Desktop `Notification` API push when permission arrives and page not focused. Not described in any design doc. |
| TokenUsage.tsx:25-36, 75-89 | UNSPEC'd | Calibration persistence via localStorage (`mclaude.costCalibration`) and save/reset logic. While calibration is spec'd in ui-spec.md:863, the localStorage persistence details are not explicitly described. This is clearly necessary infra. INFRA classification. |
| Settings.tsx:61-80 | UNSPEC'd | "Reset Client Cache" advanced section in Settings. Not described in ui-spec.md Settings screen spec. |
| DashboardScreen.tsx:88-89 | UNSPEC'd | `showChips` logic: chips shown when `groups.length > 2`. Filter chips are grouped by project name, not tmux session name per spec. This is a spec deviation (GAP reported in Phase 1) but the code exists. |

### Summary

- Implemented: 51
- Gap: 9
- Partial: 11
- Infra: 7
- Unspec'd: 9
- Dead: 0
