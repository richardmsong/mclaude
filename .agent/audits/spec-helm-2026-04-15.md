## Run: 2026-04-15T00:00:00Z

### Phase 1 — Spec → Code

| Spec (doc:line) | Spec text | Code location | Verdict | Notes |
|-----------------|-----------|---------------|---------|-------|
| plan-github-oauth.md:147-176 | Instance configuration Helm values: `controlPlane.externalUrl`, `controlPlane.providers[]` with id, type, displayName, baseUrl, apiUrl, clientId, clientSecretRef, scopes | values.yaml:87-102 | IMPLEMENTED | `externalUrl: ""` default with comment; `providers: []` default with full schema example commented inline |
| plan-github-oauth.md:150 | `controlPlane.externalUrl: "https://mclaude.internal"` — explicit, not derived from ingress.host | values.yaml:87-90 | IMPLEMENTED | Value present with comment: "Required — control-plane exits on startup if EXTERNAL_URL is empty" |
| plan-github-oauth.md:182-192 | Callback URL `{externalUrl}/auth/providers/{id}/callback`; control-plane reads external base URL from `controlPlane.externalUrl`, injected as `EXTERNAL_URL` env var | control-plane-deployment.yaml:114-115 | IMPLEMENTED | `EXTERNAL_URL` env var set from `.Values.controlPlane.externalUrl` |
| plan-github-oauth.md:192 | **Required — control-plane exits on startup if `EXTERNAL_URL` is empty**. The Helm chart template also uses `required` to fail at install time | control-plane-deployment.yaml:115 | IMPLEMENTED | `required "controlPlane.externalUrl is required" .Values.controlPlane.externalUrl` — chart fails at install if unset |
| plan-github-oauth.md:212 | "The Helm chart creates a ConfigMap named `{release}-provider-config` containing a `providers.json` key (JSON-serialized from `controlPlane.providers[]`)" | provider-config-configmap.yaml:1-12 | IMPLEMENTED | ConfigMap `{{ include "mclaude.fullname" . }}-provider-config` with `providers.json` key via `toJson` |
| plan-github-oauth.md:212 | "The control-plane Deployment mounts this ConfigMap as a volume at `/etc/mclaude/providers.json`" | control-plane-deployment.yaml:116-121, 139-143 | IMPLEMENTED | volume `provider-config` from ConfigMap, volumeMount at `/etc/mclaude/providers.json` with `subPath: providers.json` |
| plan-github-oauth.md:212 | "`controlPlane.externalUrl` is injected as the `EXTERNAL_URL` environment variable" | control-plane-deployment.yaml:114-115 | IMPLEMENTED | env var `EXTERNAL_URL` set with `required` guard |
| plan-github-oauth.md:212 | "If `providers.json` is empty or missing (no providers configured), the control-plane starts normally" | provider-config-configmap.yaml:1 | IMPLEMENTED | ConfigMap template guarded with `{{- if and .Values.controlPlane.enabled .Values.controlPlane.providers }}` — ConfigMap not created when `providers` is empty list |
| plan-github-oauth.md:212 | Conditional volume mount — only mounted when providers are configured | control-plane-deployment.yaml:116-121, 139-143 | IMPLEMENTED | Both `volumeMounts` and `volumes` blocks are guarded with `{{- if .Values.controlPlane.providers }}` |
| plan-github-oauth.md:429-431 | "New values: Each provider instance is an entry in `controlPlane.providers[]` with its own K8s Secret for the client secret" | values.yaml:93-102 | IMPLEMENTED | `providers: []` with commented example showing all fields including `clientSecretRef` |
| plan-github-oauth.md:433-436 | "New Secrets (one per provider instance): `github-oauth-secret`, `ghes-oauth-secret`, `gitlab-oauth-secret`" | values.yaml:93-102 | PARTIAL | `clientSecretRef` field in providers schema shows the convention, but no Helm template creates these provider-specific secrets — user must create them manually. Spec says "New Secrets" but does not explicitly say Helm creates them (it only says they are referenced). This is consistent with the `existingSecret` pattern used elsewhere. |
| plan-k8s-integration.md:547-590 | MCProject CRD spec: apiVersion `mclaude.io/v1alpha1`, spec fields `userId`, `projectId`, `gitUrl`, status `phase`, `userNamespace`, `conditions`, `lastReconciledAt` | mcproject-crd.yaml:1-90 | IMPLEMENTED | CRD fully matches the spec schema |
| plan-k8s-integration.md:615-617 | ClusterRole rules: namespaces, secrets, configmaps, serviceaccounts, PVCs, deployments, roles, rolebindings, MCProject CRD | clusterrole.yaml:1-47 | IMPLEMENTED | All required resource/verb combinations present |
| plan-k8s-integration.md:139-140 | NATS subject structure, KV bucket `mclaude-sessions`, key `{userId}/{location}/{projectId}/{sessionId}` | — | — | Note: state-schema.md (canonical) uses dot-separated `{userId}.{projectId}.{sessionId}` without `{location}`. Helm chart doesn't set this directly — not a Helm gap. |
| plan-k8s-integration.md:172-174 | NATS server: port 4222, http_port 8222, websocket port 8080, max_payload 8MB, jetstream store_dir /data/jetstream | nats-configmap.yaml:15-32 | IMPLEMENTED | All ports and settings rendered from values |
| plan-k8s-integration.md:1112-1125 | nginx ingress routing: `/auth`, `/api`, `/scim` → control-plane; `/` → SPA | ingress.yaml:28-60 | IMPLEMENTED | All four path rules present |
| plan-k8s-integration.md:740-745 | Pod volumes: project-data PVC, claude-home emptyDir, user-config ConfigMap, user-secrets Secret, nix-store PVC | session-agent-pod-template.yaml:1-23 | IMPLEMENTED | All five ConfigMap keys present (image, imagePullPolicy, terminationGracePeriodSeconds, resourcesJson, projectPvcSize, projectPvcStorageClass, nixPvcSize, nixPvcStorageClass) |

### Phase 2 — Code → Spec

| File:lines | Classification | Explanation |
|------------|---------------|-------------|
| provider-config-configmap.yaml:1 | INFRA | `{{- if and .Values.controlPlane.enabled .Values.controlPlane.providers }}` — guards template, consistent with spec that ConfigMap only exists when providers configured |
| control-plane-deployment.yaml:116-121 | INFRA | `{{- if .Values.controlPlane.providers }}` volumeMounts block — conditional per spec |
| control-plane-deployment.yaml:139-143 | INFRA | `{{- if .Values.controlPlane.providers }}` volumes block — conditional per spec |
| values.yaml:87-102 | INFRA | `controlPlane.externalUrl` and `controlPlane.providers` values with inline documentation — all spec'd |
| values.yaml:93-102 | INFRA | `providers: []` default with commented example — spec-driven |
| control-plane-deployment.yaml:25-46 | INFRA | dbmate init container for migrations — spec'd in plan-k8s-integration.md "Schema migrations managed by dbmate init container" |
| control-plane-deployment.yaml:67-115 | INFRA | All other env vars (LOG_LEVEL, HTTP_PORT, JWT_EXPIRY_SECONDS, NATS_URL, DATABASE_URL, ADMIN_TOKEN, NATS_OPERATOR_*) — all spec'd in plan-k8s-integration.md |
| nats-configmap.yaml:1-40 | INFRA | NATS server config — all fields spec'd in plan-k8s-integration.md |
| nats-statefulset.yaml:1-91 | INFRA | NATS StatefulSet with persistence — spec'd in plan-k8s-integration.md |
| nats-services.yaml:1-50 | INFRA | NATS client + headless services — spec'd in plan-k8s-integration.md |
| postgres-statefulset.yaml:1-103 | INFRA | Postgres StatefulSet — spec'd in plan-k8s-integration.md |
| postgres-service.yaml (not read) | INFRA | Postgres service — spec'd |
| spa-deployment.yaml:1-52 | INFRA | SPA Deployment — spec'd in plan-k8s-integration.md |
| spa-service.yaml (not read) | INFRA | SPA service — spec'd |
| ingress.yaml:1-61 | INFRA | Main ingress with /auth, /api, /scim, / routing — spec'd in plan-k8s-integration.md |
| nats-ws-ingress.yaml:1-43 | INFRA | Separate NATS WebSocket ingress — spec'd in plan-k8s-integration.md (natsHost) |
| mcproject-crd.yaml:1-90 | INFRA | MCProject CRD definition — spec'd in plan-k8s-integration.md |
| clusterrole.yaml:1-47 | INFRA | ClusterRole for control-plane — spec'd in plan-k8s-integration.md |
| clusterrolebinding.yaml:1-17 | INFRA | ClusterRoleBinding — standard RBAC plumbing for ClusterRole |
| serviceaccount.yaml:1-15 | INFRA | ServiceAccount for control-plane — spec'd |
| namespace.yaml:1-9 | INFRA | mclaude-system namespace — spec'd |
| session-agent-pod-template.yaml:1-23 | INFRA | Session agent template ConfigMap — spec'd in plan-state-schema.md |
| _helpers.tpl:1-124 | INFRA | Standard Helm helper templates (naming, labels, image resolution, security context) — necessary boilerplate |
| values.yaml:1-247 | INFRA | All values are spec-driven per plan-k8s-integration.md and plan-github-oauth.md |
| Chart.yaml | INFRA | Chart metadata — standard Helm boilerplate |
| Makefile | INFRA | Build/dev tooling — not production runtime |
| policies/deny.rego | UNSPEC'd | OPA deny policy for Helm resources — not described in any design doc. Likely needed for security validation; spec doesn't describe OPA/Conftest validation in the Helm chart. |
| scripts/check-resources.py | UNSPEC'd | Resource check script — not described in any design doc. Likely CI helper. |
| scripts/dev.sh | UNSPEC'd | Dev script — not described in any design doc. |
| scripts/e2e.sh | UNSPEC'd | E2E script — not described in any design doc. |
| values-airgap.yaml | UNSPEC'd | Air-gap values override — plan-k8s-integration.md mentions air-gapped environments and Artifactory but does not specify this values file. |
| values-aks.yaml | UNSPEC'd | AKS-specific values — no design doc specifies this file. |
| values-e2e.yaml | UNSPEC'd | E2E values — no design doc specifies this file. |
| values-dev.yaml | UNSPEC'd | Dev values — no design doc specifies this file. |
| values-k3d-ghcr.yaml | UNSPEC'd | k3d+GHCR values — no design doc specifies this file. |
| templates/tests/test-smoke.yaml | INFRA | Helm test hook for smoke test — standard Helm testing pattern, not described in spec but consistent with "Integration tests" section |
| templates/NOTES.txt | INFRA | Helm post-install notes — standard Helm boilerplate |

### Summary

- Implemented: 14
- Gap: 0
- Partial: 1
- Infra: 24
- Unspec'd: 7
- Dead: 0
